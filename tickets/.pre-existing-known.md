# Known pre-existing failures (tess)

- `packages/integration-tests/src/scenarios/push-wake-e2e.integration.ts > E2E push-wake over the control network > wakes a member whose authorization and address were learned by control-DB replication, not local seeding` → two distinct defects, both in-flight | 2026-07-29 (intermittent, ~7/25 when run alone via `-t`; passes in whole-file runs)
  - split write / `PartialCommitError` (2/25 runs) → bug-control-db-stale-revision-not-retryable
  - `Timeout waiting for S resolves Rx's address record via replication` (5/25 runs) → bug-control-db-rx-record-never-converges-on-sender
