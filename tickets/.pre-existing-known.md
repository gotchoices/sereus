# Known pre-existing failures (tess)

- `packages/integration-tests/src/scenarios/push-wake-e2e.integration.ts > E2E push-wake over the control network > delivers a wake to a NAT'd receiver over a circuit-relay (signaling-first) dial` → strand-delegate-peer-relay-admission | in-flight | re-confirmed 2026-07-30 (deterministic at HEAD, alone and in whole-file runs; NOT contention, NOT build drift)
  - `UnsupportedListenAddressesError` / `UnexpectedEOFError` in `@libp2p/circuit-relay-v2` reservation-store during `libp2p.start()`, thrown from `Rx.addStrand`. Root cause CONFIRMED: the strand node's derived (unattested) transport peerId is denied by the relay-cum-control-node's membership connection gater. The `docs/strands.md:76-79` design question is now RESOLVED in the implement ticket (a party control node running a relay serves its own members' delegate peerIds and nobody else); the fix is a member-announced delegate admission grant carried on the existing strand-addr RPC.

## Resolved in place (no ticket — root cause landed)

- `strand-formation-e2e.integration.ts > E2E Strand Formation > Phase 2: Strand instance lifecycle > should form a strand with three parties` — was `Timeout waiting for data replicates to Bob after 15000ms`. Root cause was **not** the control-DB replication class it was suspected of: a three-party strand replicates each block to only 2 of the 3 members (`DEFAULT_STRAND_CLUSTER_SIZE` = 2), and the member outside the cohort had no way to obtain a block it had never seen — `BlockStorage.getBlock` returned before `ensureRevision` when local metadata was empty, so the corroborated cluster revision was established and then discarded. Fixed upstream by `../optimystic` `559df6a` ("let a read acquire a block the node has never seen"), which is in optimystic HEAD; the third member now acquires the block at read time (`cluster-fetch:synced`). Verified 2026-07-29 at sereus HEAD: **13/13 green** — 8/8 alone via `-t` (1.3–1.6 s each, well inside the unchanged 15 s budget), 4/4 whole-file, 1/1 in a mixed run behind `convergence-stress`/`multi-party-sync`/`control-cohort-auto-convergence`/`happy-path`. The remaining behaviour — that adding parties never adds copies, so members read from a cohort instead of holding data — is not a defect and is tracked as `backlog/debt-strand-replication-breadth-ignores-party-count` plus the human decision in `blocked/replication-breadth-two-signoff`.

- `push-wake-e2e.integration.ts > E2E push-wake over the control network > wakes a member whose authorization and address were learned by control-DB replication, not local seeding` — was intermittent (6 failures in 20 when run alone via `-t`; 4 in 10 in the original measurement) while the control database replicated each block to only 2 nodes, which left a member that missed a write dependent on read repair, and read repair cannot converge at a two-member cohort. Fixed by `control-db-replicates-to-whole-party` (control breadth is now `CONTROL_REPLICATION_BREADTH` = 16, i.e. the whole party); **20/20 green alone via `-t`, plus 2/2 in whole-suite runs**, verified 2026-07-29 by `control-db-replicates-to-whole-party-validation`. The earlier stale-revision retry work (`bug-control-db-stale-revision-not-retryable`, `bug-control-db-rx-record-never-converges-on-sender`) reduced but did not remove the failure rate. The unexplained 1-in-20 failure previously listed here (log lost) has not recurred.

- `cadre-host-bootstrap.integration.ts > install + start → /api/status returns the live service snapshot`, both `cadre-host-origin-guard.integration.ts` accept-cases, and `cadre-host-trust-circle.integration.ts > completes issue → list → redeem → list → remove cycle` — all four were the `createTestCadreHost` harness diverging from `bin/host.ts`: `defaultFakeCadreNode()` was missing `listAuthorizedMembers`/`isAuthorizedMember` (→ `TypeError` → bare 500), and the harness never ran the `ensureSelfLabel` startup step (→ owner's own row absent from the listing). Fixed 2026-07-29; `yarn typecheck` in `packages/integration-tests` had been failing on the same gap and is now clean.
