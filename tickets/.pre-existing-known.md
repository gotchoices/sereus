# Known pre-existing failures (tess)

- `packages/integration-tests/src/scenarios/push-wake-e2e.integration.ts > E2E push-wake over the control network > wakes a member whose authorization and address were learned by control-DB replication, not local seeding` → remaining defect in-flight | 2026-07-29 (intermittent, 6/20 when run alone via `-t` after the stale-revision retry fix landed; passes in whole-file runs)
  - `Timeout waiting for S resolves Rx's address record via replication` (2/20 runs), and `SyncRetryExhaustedError` after exhausting 10 retries — sometimes benign (first tree, nothing durably committed; 2/20), sometimes still producing a `PartialCommitError` when exhaustion hits a later tree in `commitDirtyTreesLegacy` after an earlier tree already committed (1/20) → bug-control-db-rx-record-never-converges-on-sender (root cause: control-DB replication read-path never converges within the retry budget; see `tickets/complete/bug-control-db-stale-revision-not-retryable.md` review findings for the full evidence and the open question of whether the `PartialCommitError` recurrence needs its own follow-up)
  - 1 further failure in the 20-run sample not matching any of the above shapes; its log was lost before inspection (session scratchpad), did not recur in this session's runs

- `packages/integration-tests/src/scenarios/push-wake-e2e.integration.ts > E2E push-wake over the control network > delivers a wake to a NAT'd receiver over a circuit-relay (signaling-first) dial` → bug-strand-node-relay-reservation-denied-by-membership-gate | in-flight | 2026-07-29 (deterministic at HEAD, alone and in whole-file runs; NOT contention, NOT build drift)
  - `UnsupportedListenAddressesError` / `UnexpectedEOFError` in `@libp2p/circuit-relay-v2` reservation-store during `libp2p.start()`, thrown from `Rx.addStrand`. Root cause CONFIRMED: the strand node's derived (unattested) transport peerId is denied by the relay-cum-control-node's membership connection gater. Needs a design decision first (`docs/strands.md:76-79` open question) — see the ticket's "Design constraints".

- `packages/integration-tests/src/scenarios/strand-formation-e2e.integration.ts > E2E Strand Formation > Phase 2: Strand instance lifecycle > should form a strand with three parties` → bug-strand-formation-three-party-replication-timeout | in-flight | 2026-07-29 (NOT re-verified — triage pass hit its budget; re-run to confirm before investigating)
  - `Timeout waiting for data replicates to Bob after 15000ms`. Suspected same class as bug-control-db-rx-record-never-converges-on-sender but a different collection/cohort (strand DB, not control DB); fold in and delete if one shared root cause.

## Resolved in place (no ticket — root cause landed)

- `cadre-host-bootstrap.integration.ts > install + start → /api/status returns the live service snapshot`, both `cadre-host-origin-guard.integration.ts` accept-cases, and `cadre-host-trust-circle.integration.ts > completes issue → list → redeem → list → remove cycle` — all four were the `createTestCadreHost` harness diverging from `bin/host.ts`: `defaultFakeCadreNode()` was missing `listAuthorizedMembers`/`isAuthorizedMember` (→ `TypeError` → bare 500), and the harness never ran the `ensureSelfLabel` startup step (→ owner's own row absent from the listing). Fixed 2026-07-29; `yarn typecheck` in `packages/integration-tests` had been failing on the same gap and is now clean.
