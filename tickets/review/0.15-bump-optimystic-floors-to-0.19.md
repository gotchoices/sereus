description: The database library we build on published a new version while we were working, so the minimum versions our packages declare were behind again — this lands the fix and re-confirms nothing else moved.
files: packages/cadre-cli/package.json, packages/cadre-core/package.json, packages/quereus-plugin-sereus/package.json, packages/integration-tests/package.json, packages/reference-app-web/package.json, packages/reference-app-rn/package.json, packages/reference-app-ns/package.json, yarn.lock, docs/releasing.md, tickets/.pre-existing-error.md
difficulty: easy
---

# Optimystic floors bumped `^0.18.0` → `^0.19.0` — done, gate green, nothing else moved

## What changed

`yarn upgrade:optimystic` (ncu, workspace-filtered) rewrote all 22 `@optimystic/*` ranges from
`^0.18.0` to `^0.19.0` across the seven consuming `package.json` files listed above. `yarn install`
followed; `yarn.lock` moved only the `@optimystic/*` version/checksum lines — `git diff yarn.lock`
shows zero lines outside those entries, confirming no transitive package moved as a side effect.

Two lines in `docs/releasing.md` also changed (see "Doc fix" below).

## Verification — everything green except one pre-existing, unrelated build target

- `yarn check:dep-ranges` → `check-dep-ranges: all declared ranges admit their linked workspace
  version (9 linked package(s))`. Was 22 too-old ranges before.
- `yarn dep-check` (knip + the range gate) → exit 0.
- `yarn typecheck` → exit 0 repo-wide, including the two coverage checks
  (`check-vitest-typecheck-coverage`, `check-test-file-typecheck-coverage`).
- `yarn lint` → exit 0, zero output.
- `yarn build`, isolated per workspace — **all pass except `reference-app-web`**:
  `cadre-core`, `cadre-cli`, `cadre-host`, `cadre-provider`, `quereus-plugin-sereus`,
  `integration-tests` all exit 0. `reference-app-rn`/`reference-app-ns` have no `build` script
  (typecheck-only apps).
  - `reference-app-web`'s **production Vite bundle** fails: `[vite]: Rollup failed to resolve
    import "temporal-polyfill" from ".../quereus/packages/quereus/dist/src/types/temporal-types.js"`.
    `temporal-polyfill` is a real `@quereus/quereus` dependency installed only in `../quereus`'s own
    `node_modules` (a separate repo, `link:`-resolved) — never in Sereus's own tree — so Rollup can't
    resolve it while bundling. **Pre-existing and unrelated to this ticket**: this ticket's diff
    touches only `@optimystic/*`, and the `yarn.lock` diff proves nothing `@quereus/quereus`- or
    `temporal-polyfill`-adjacent moved. Not previously tracked anywhere in `tickets/` (searched all
    stages) — filed as `tickets/.pre-existing-error.md` per the pre-existing-failure protocol so the
    triage pipeline picks it up. **Reviewer: this is a real, currently-broken build target that
    deserves a `fix/` ticket once triaged — don't wave it off, but it isn't this ticket's to fix.**
    `reference-app-web`'s own vitest suite (`yarn workspace @serfab/reference-app-web run test`) is
    unaffected and was not touched by this failure.

### Test suites — confirms the bump changed nothing

- **`cadre-core`**: 5 failed / 1501 passed / 1 skipped (1507 total) — same as the 2026-08-03
  baseline. Both failing files (`control-revocation-reissue.spec.ts`,
  `control-revocation-replay.spec.ts`) are the already-tracked
  `10-revocation-reissue-same-pk-update-unique-collision` Quereus DML defect
  (`tickets/.pre-existing-known.md`); not re-filed.
- **`integration-tests`**: **241 tests total — exact match** to the 2026-08-03 baseline count. Run
  in 6 batches (not 5 — see note) because the suite exceeds a single 10-minute window; each batch's
  file list is in the handoff log if needed, or just re-split `src/scenarios/*.integration.ts` (5
  batches) plus `test/*.spec.ts` + `../../test-harness/build-freshness.spec.ts` (1 batch — this last
  group is what makes it 6, and it was 100% green, 70/70).
  - ~11 failed (doc's baseline was ~13; the doc itself says to judge by fingerprint shape, not exact
    count, since these are routing races that move between runs and batchings). Every failure seen
    was one of the already-documented multi-machine fingerprints: `peers-unreachable`
    (`control-cohort-edge-carries-data`), `… header block read as absent …`
    (`harness-party-control-cohort`, `push-wake-e2e`, `strand-addr-seed-convergence`,
    `strand-membership-closed-strand-e2e` ×2, `control-delete-while-alone-convergence`'s second
    case), a `waitUntil` timeout (`control-write-degraded-cohort-member`,
    `strand-membership-closed-strand-e2e`'s third case), and one `expect(...).toBe(true)` on stale
    membership state (`control-delete-while-alone-convergence`'s first case). No new fingerprint
    class appeared. **Root cause of the header-absent class is tracked upstream**,
    `tickets/blocked/control-coordinator-answers-absent-without-asking-cohort` — nothing here is new.
  - **Measurement caveat** (matches the ticket's own warning): `../quereus` carries uncommitted,
    non-compiling `alter-table`/`add-constraint` work the whole session, and its automation edits a
    *different* set of files every ~30s (watched it happen live via `git status` between commands).
    `yarn workspace @quereus/quereus build` cannot be run there. Its `dist/` is a clean build of that
    repo's committed HEAD (`89c740ce`), so — same workaround the 2026-08-03 baseline used — I advanced
    every file's mtime under `../quereus/packages/quereus/dist` (content byte-for-byte unchanged,
    confirmed no `git diff` in that repo from the touch) to clear the stale-build guard without
    building or touching `../quereus` source. **Do not revert or stash anything in `../quereus`** —
    its in-flight work is someone else's, untouched here.

## Doc fix (in scope per the ticket)

`docs/releasing.md`'s "Recommendation" section had drifted out of sync with its own later
"Dependency floors" subsection: the top-line recommendation still read "with the declared dependency
floors left at `^0.18.0`" and the "why a minor bump" parenthetical still read `^0.14.1 → ^0.18.0`,
both now factually wrong once this ticket's floor bump lands. Corrected both to `^0.19.0`. Left the
"moved two minor versions" phrasing alone — that count looks like a pre-existing looseness unrelated
to this ticket (0.14→0.18 was already 4 point-releases apart, not two), out of scope to adjudicate
here.

`docs/STATUS.md`, `tickets/blocked/optimystic-testing-barrel-breaks-consumer-install`, and
`tickets/blocked/cut-the-interim-release` were all read and already read true post-bump — their
`^0.18.0` mentions are dated historical records (e.g. "As of 2026-08-03 it fails... at the `^0.18.0`
floor this repo declares"), not live claims, and were left alone. `docs/releasing.md`'s runbook step
2 already correctly said "moves them to `^0.19.0`" and needed no change.

## What a reviewer should check

- `yarn check:dep-ranges` reports 0 too-old ranges (fast, deterministic — the actual acceptance
  gate).
- `git diff yarn.lock` touches only `@optimystic/*` entries — if it doesn't, something drifted that
  shouldn't have.
- The two `docs/releasing.md` edits read correctly in context (lines ~151, ~156).
- `tickets/.pre-existing-error.md` — decide whether the reference-app-web/temporal-polyfill build
  failure needs anything beyond what's filed; it's real and reproducible but predates this ticket.
- Not independently re-verified here: `packages/quereus-plugin-sereus`, `cadre-host`, `cadre-cli`,
  `cadre-provider` test suites (only their *builds* were confirmed green) — the ticket's TODO only
  asked to re-run `cadre-core` and `integration-tests`, so I didn't expand scope, but flagging the gap
  since a reviewer may want the full-repo picture.
