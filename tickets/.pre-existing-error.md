# Pre-existing / environmental test failure

## Command
```
cd packages/integration-tests
yarn vitest run control-db-two-node-convergence
```
(package: `@serfab/integration-tests`)

## Failing test
- File: `packages/integration-tests/src/scenarios/control-db-two-node-convergence.integration.ts`
- Name: `Two-node control-DB convergence > replicates an authority-written CadrePeer row from node A to node B over the live control network`

## Error excerpt (differs run-to-run — non-deterministic)
Run 1:
```
Error: Some peers did not complete: ...(in-flight)
  cause=Transaction rejected by validators (2/2 rejected):
    <peer>: membership-not-admitted:low-confidence-downsize;
    <peer>: membership-not-admitted:low-confidence-downsize
  ❯ ClusterCoordinator.executeTransaction ../../../optimystic/packages/db-p2p/src/repo/cluster-coordinator.ts:337:10
```
Run 2 (same test, re-run):
```
Caused by: StreamResetError: The stream has been reset
  ❯ YamuxStream.onRemoteReset .../@libp2p/utils/.../abstract-message-stream.js:253:21
```

## Why this is pre-existing / not from this ticket
- Every failing frame is inside the **external `../optimystic` linked workspace**
  (`db-p2p` `ClusterCoordinator`, `db-core` `NetworkTransactor`, Yamux transport) —
  the live 2-node p2p replication substrate. None of the failure touches
  `CadreControl.CadrePeer` or any cadre-core code this ticket changed.
- The error is `membership-not-admitted:low-confidence-downsize` (an optimystic
  **cluster-node** sizing/confidence decision) and `stream has been reset`
  (transport) — neither correlates to this ticket's change (two nullable columns +
  pure-equality voucher predicates on the SQL row).
- **Non-deterministic**: two consecutive runs failed with two different p2p-layer
  signatures — a flaky live-network convergence test, not a deterministic schema
  rejection.
- **Already characterized as external in-repo**: `docs/STATUS.md` (the Option-B
  membership section, ~line 552) already records this 2-of-2 validator-rejection as
  "upstream of the count (peer selection / protocol negotiation) ... optimystic-side
  networking work, not a one-line sereus change." It is not yet in
  `tickets/.pre-existing-known.md`, so it is reported here; triage may prefer to add it
  to the known list / route to `blocked/` (external `../optimystic` workspace) rather
  than open a sereus fix.
- The **single-node** authority write path that exercises the exact new voucher
  constraint end-to-end — `cadre-host-authority-node.integration.ts` "accept-phone
  authorizes a peer, then removePeer deletes it (full add→remove cycle)" — **passes**,
  as do all 64 cadre-core unit/constraint specs. So the voucher insert/delete through
  the new `AuthorizedInsert`/`AuthorizedDelete` predicates is proven working; only the
  multi-node p2p convergence leg is flaky.
