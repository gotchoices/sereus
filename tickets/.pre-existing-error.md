# Pre-existing failure: control-founding-consult-budget genesis floor trips (3 consults, floor needs > 3)

Seen during the review pass of `control-write-retry-torn-final-and-degraded-rerun`, 2026-09-17.

## Command

From the repo root:

```
yarn workspace @serfab/cadre-core test                                                   # full suite: 1 failed | 2212 passed
yarn workspace @serfab/cadre-core test test/control-founding-consult-budget.spec.ts      # isolated: same failure, reproducible
```

Stale-build guard green both times. `../optimystic` HEAD `8a0b48c7` (tickets-only commit on top of `8a0ad39b`), with uncommitted edits in `packages/db-p2p/src/cluster/race-resolution.ts`, `docs/correctness.md`, `packages/db-p2p/docs/cluster.md` and two db-p2p specs (that repo's own runner, in flight).

Full-suite log: `tickets/.logs/control-write-retry-torn-final-and-degraded-rerun.review-core.log`.

## Failing test

`packages/cadre-core/test/control-founding-consult-budget.spec.ts` > "control database founding, cohort consult and commit budget" > "stays within its consult and commit budgets from cold start through an idle reconcile pass"

```
AssertionError: genesis issued only 3 cohort consults, far below (measured 7 consults over 3 blocks and 4 commits on 2026-09-17 at optimystic 03ffadc4; ...). This run's consults per block: default/cadrecontrol/OwnerKey×1, default/cadrecontrol/OwnerKey/index/_uniq_7.stampid×1, default/cadrecontrol/Revocation×1. Either the counter no longer sees this path ..., or the cost genuinely improved — in which case re-measure and TIGHTEN the budget.: expected 3 to be greater than 3
 ❯ expectWithinBudget test/control-founding-consult-budget.spec.ts:534:5
```

The other phases in the same run: cold start 26 consults, foundStrand (control) 7, foundStrand (strand) 21, reconcile 4.

## Why it is pre-existing

This is an anti-vacuity FLOOR (the cost went down, not up). The change under review only alters how the control-write retry classifies a failure *after* a write fails; no write failed or retried in this run, and a retry could only add consults, never remove them. `tickets/.pre-existing-known.md` lists this test against `budget-floors-trip-while-optimystic-block-floor-work-is-in-flight`, but that ticket is now in `complete/` (re-baselined at optimystic `03ffadc4`), so this is a fresh drift. Likely cause is upstream optimystic commits since then (`13586033`, `2fdb3b97`, `8a0ad39b`); not bisected.
