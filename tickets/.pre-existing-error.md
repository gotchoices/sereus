# Pre-existing failure — `strand-solo-write-budget` insert phase over its ceiling

Reported by the review pass of `restore-formation-usage-token-index`, 2026-09-17.

## Command

From the repo root: `yarn workspace @serfab/cadre-core test` (full cadre-core unit suite). Result: 1 failed, 2197 passed, 1 skipped, across 135 files. Log: `tickets/.logs/restore-formation-usage-token-index.review-core.log`.

## Failing test

`packages/cadre-core/test/strand-solo-write-budget.spec.ts` › "solo strand write budget" › "stays within its operation budgets"

```
AssertionError: solo insert issued 99 raw-storage operations, over the budget of 90 (measured 80 ops over 3 distinct blocks on 2026-09-17 at optimystic 03ffadc4). ... This run, calls/distinct-blocks by method: saveMetadata 22/3, saveMaterializedBlock 17/3, savePendingTransaction 11/3, saveRevision 11/3, promotePendingTransaction 11/3, deletePendingTransaction 11/3, saveBlockProof 11/3, listPendingTransactions 3/3, getMetadata 2/2.: expected 99 to be less than or equal to 90
 ❯ expectWithinBudget test/strand-solo-write-budget.spec.ts:267:4
```

The launch phase (88 ops, 17 blocks) and the select phase (0) passed. Only the insert ceiling tripped (`INSERT` at line 103: `ops: 80, opBudget: 90`).

## Why it is not this ticket's

- **What the spec counts rules out this diff.** The spec counts raw-storage calls on the STRAND scope only, and asserts before `addStrand` that the control scope added nothing (`expect(counter.snapshot().total).toBe(0)`, line ~191). That assertion passed. This ticket's only `cadre-core` source change is to the CadreControl schema (the `FormationUsageByToken` index and a comment next to it), which lives in the control scope the spec excludes.
- **The upstream it was measured against was in the middle of a change.** At the time of the run (18:40–18:42), `../optimystic` sat at `bbecaf28` with UNCOMMITTED edits by a concurrent runner to `packages/db-p2p/src/storage/{block-storage,i-block-storage,storage-repo,struct}.ts`, a new untracked `storage/pending-claim.ts`, and `repo/coordinator-repo.ts`. That is the raw-storage layer this spec counts, and `db-p2p`'s dist had been rebuilt from that work-in-progress at 18:39:59. The two excess methods (`saveMetadata` 22 against a baseline of 14, `saveMaterializedBlock` 17) are block-storage writes.
- **This is not the known entry.** `tickets/.pre-existing-known.md` already lists this test under `budget-floors-trip-while-optimystic-block-floor-work-is-in-flight`, but that entry is about FLOOR trips (cost fell below half its baseline), and that ticket is now in `complete/`. This failure is a CEILING trip (cost rose), measured against a different upstream state.

## What would settle it

Re-run `yarn workspace @serfab/cadre-core test --run strand-solo-write-budget` once `../optimystic` has committed its current `db-p2p` storage work and built cleanly. If 99 (or anything over 90) persists at a committed upstream, that change made the solo insert more expensive, and either upstream or the budget needs a decision. If it drops back to about 80, this was only the half-finished upstream.
