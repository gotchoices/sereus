# Pre-existing test failure: reference-app-ns `cadre-vm.spec.ts` event-list assertions

## Command

From the repo root: `yarn workspace @serfab/reference-app-ns test` (also `yarn workspace @serfab/reference-app-ns vitest run test/cadre-vm.spec.ts`).

## Failing tests

`packages/reference-app-ns/test/cadre-vm.spec.ts`:

- `the constructor > adopts a running node, reading its peer id, strands and events` (line 118)
- `stop > unbinds every strand event before dropping the node`

```
AssertionError: expected [ 'getStrands', …(4) ] to deeply equal [ 'getStrands', …(3) ]
  [
    "getStrands",
    "on:strand:started",
    "on:strand:stopped",
+   "on:strand:writable",
    "on:strand:error",
  ]

AssertionError: expected [ 'off:strand:started', …(4) ] to deeply equal [ 'off:strand:started', …(3) ]
```

## Why pre-existing

Commit `0fe8a79` (`joining-machine-writes-before-first-sync-fork-tables`) added a `strand:writable` subscription to `packages/reference-app-ns/src/cadre-vm.ts` and did not update the two assertions in `cadre-vm.spec.ts` that list the subscribed events; the ticket that surfaced this (`rn-chat-poll-overlaps-slow-reads`) touches only `chat-vm.ts` in that package, which `cadre-vm.spec.ts` does not import.
