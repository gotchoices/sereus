## Pre-existing test failure(s)

**Command:** `cd packages/cadre-core && yarn test` (full package suite)

**Found while working:** `21-debt-strand-spec-helpers-duplicated` (implement stage) — a
pure test-helper dedup ticket touching only `strand-*.spec.ts` files and a new
`strand-spec-helpers.ts` module. Confirmed via `git diff e6f284f..HEAD --stat` (e6f284f =
this ticket's plan commit) that the ticket's diff never touches
`control-revocation-reissue.spec.ts`, `control-revocation-replay.spec.ts`, or any source
file those tests exercise.

**Failing tests:**

1. `test/control-revocation-reissue.spec.ts > Revocation: owner-signed tombstone re-issue > an owner-signed counter bump lands, leaving the identity triple untouched`
2. `test/control-revocation-reissue.spec.ts > Revocation: owner-signed tombstone re-issue > the counter moves strictly upward: equal and lower are refused, higher lands (ReissueOnly)`
3. `test/control-revocation-reissue.spec.ts > Revocation: owner-signed tombstone re-issue > reissueRevocations bumps a whole batch in one transaction, and one stale counter rolls the batch back`
4. `test/control-revocation-reissue.spec.ts > Revocation: owner-signed tombstone re-issue > a bump signed over the WRONG digest or by a non-owner is refused (AuthorizedReissue)`
5. `test/control-revocation-replay.spec.ts > Revocation: remove-then-replay resurrection is closed > Revocation: a tombstone is permanent — delete refused, and update may move only the counter (NoDelete / ReissueOnly / AuthorizedReissue)`

**Excerpt:**

```
FAIL  test/control-revocation-reissue.spec.ts > ... > an owner-signed counter bump lands, leaving the identity triple untouched
ConstraintError: UNIQUE constraint failed: Revocation.TableName, Revocation.StampId
 ❯ new ConstraintError ../../../quereus/packages/quereus/src/common/errors.ts:36:3
 ❯ processUpdateRow ../../../quereus/packages/quereus/src/runtime/emit/dml-executor.ts:1158:10
 ...

FAIL  test/control-revocation-reissue.spec.ts > ... > a bump signed over the WRONG digest or by a non-owner is refused (AuthorizedReissue)
AssertionError: expected [Function] to throw error matching /CHECK constraint failed: (AuthorizedR…/ but got 'UNIQUE constraint failed: Revocation.…'

FAIL  test/control-revocation-replay.spec.ts > ... > a tombstone is permanent — delete refused, and update may move only the counter (NoDelete / ReissueOnly / AuthorizedReissue)
AssertionError: expected [Function] to throw error matching /CHECK constraint failed: (AuthorizedR…/ but got "context.OwnerKey isn't a column"
```

**Why pre-existing:** zero overlap between this ticket's diff (5 `strand-*.spec.ts` files +
new `strand-spec-helpers.ts`) and the failing files/subsystem (`control-revocation-*.spec.ts`,
exercising owner-signed revocation reissue via `../../../quereus` DML executor). The errors
are UNIQUE/CHECK constraint mismatches inside Quereus's update executor and revocation
reissue authorization logic — unrelated to test-helper hoisting. 83/85 test files (1370/1376
tests) pass, including all 5 files this ticket touched.
