# Pre-existing lint failure (not caused by cadre-cli-seed-endpoint-auth)

## Command run

```
yarn lint
```
(monorepo-wide ESLint, run from repo root C:\projects\sereus)

## Result

```
✖ 6453 problems (5315 errors, 1138 warnings)
```

## Failing files (excerpt)

The overwhelming majority of errors come from **generated / vendored build
artifacts that should be ignored by ESLint**, not source code:

- `packages/reference-app-ns/platforms/android/app/src/main/assets/app/bundle.js`
- `.../assets/app/vendor.js`, `.../assets/app/runtime.js`, `.../assets/app/0.js`
- `.../platforms/android/app/build-tools/jsparser/js_parser.js`
- `.../assets/internal/ts_helpers.js`

These are multi-megabyte single-line minified files (error positions like
`1:2316437`), i.e. NativeScript Android build output being linted. Remaining
warnings/errors are scattered across `reference-app-web`, `cadre-host`,
`cadre-provider`, `cadre-core`, `quereus-plugin-sereus`, etc.

## Why this is pre-existing / not mine

- This ticket (`cadre-cli-seed-endpoint-auth`, review stage) changed **only**
  `.md`, `.yml`, and `.example` files (docker-compose template + operator docs).
  ESLint does not lint those file types, so the diff cannot introduce TS/JS lint
  errors.
- `packages/cadre-cli` — the package this ticket touches — does **not** appear
  anywhere in the lint error list. It lints clean.
- The error volume and the presence of generated Android bundle files indicate
  an ESLint ignore-config gap that predates this ticket.

## Work-around

None needed for this ticket — no lint-eligible files were changed.
`yarn workspace @serfab/cadre-cli test` → **50 passed (5 files)**.
