# Pre-existing failure: integration-tests typecheck (cross-package rootDir)

## Command

From `packages/integration-tests`:

```
yarn typecheck
# = tsc -p tsconfig.typecheck.json --noEmit
```

## Failure (3 errors, all in one file I did not touch)

```
src/scenarios/formation-responder-smoke.spec.ts(4,41): error TS6059: File
  'C:/projects/sereus/packages/reference-app-web/e2e/fixtures/formation-responder.ts'
  is not under 'rootDir' 'C:/projects/sereus/packages/integration-tests/src'.
../reference-app-web/e2e/fixtures/formation-responder.ts(43,49): error TS6059: File
  '.../reference-app-web/src/lib/chat-strand.ts' is not under 'rootDir' ...
../reference-app-web/e2e/fixtures/formation-responder.ts(44,55): error TS6059: File
  '.../reference-app-web/src/lib/chat-dml.ts' is not under 'rootDir' ...
```

## Why this is pre-existing (not mine)

- `formation-responder-smoke.spec.ts` is a tracked file I never modified; its own
  header says: `// TEMPORARY review smoke — DELETE after running.` (left over from the
  `formation-convergence-e2e-responder-fixture` review).
- It `import`s `../../../reference-app-web/e2e/fixtures/formation-responder.js`, i.e.
  another package's sources, which sit outside this package's `rootDir`
  (`src`) — a tsconfig/layout issue, not a code defect.
- Reproduced with my new file moved out of the tree: `yarn typecheck` still emits the
  same 3 `TS6059` errors. My new file (`strand-membership-closed-strand-e2e.integration.ts`)
  contributes **zero** typecheck errors.

## Work-around taken

None needed for my ticket — Vitest transpiles per-file with esbuild and does not run
`tsc`, so the new scenario builds and passes regardless of this `tsc --noEmit` gate.
I left the offending temporary file untouched (not mine to delete).

## Suggested triage

Delete the stale `formation-responder-smoke.spec.ts` (its header marks it for deletion)
or exclude it from `tsconfig.typecheck.json` until/if cross-package smoke is wanted.
