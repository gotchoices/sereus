## Pre-existing failures surfaced while working on svelte-check-gate

---

### 1. `reference-app-web` svelte-check type errors

**Command:** `cd packages/reference-app-web && yarn exec svelte-check`

**Failing file:** `src/lib/cadre-web.ts` (2 errors at lines 286 and 323)

**Error excerpt:**
```
ERROR "src\lib\cadre-web.ts" 286:3
  Type 'PrivateKey' is not assignable to type 'PrivateKey | undefined'.
    Type 'RSAPrivateKey' (from optimystic/db-p2p-storage-web) is not assignable
    to type 'RSAPrivateKey' (from sereus/node_modules/@libp2p/interface).
      Uint8ArrayList' is not assignable to type 'Uint8ArrayList<ArrayBufferLike>'.
        Property '[symbol]' is missing in type 'Uint8ArrayList' ...

ERROR "src\lib\cadre-web.ts" 323:34  (same root cause — incompatible PrivateKey argument)
```

**Why pre-existing:** Version mismatch between `@libp2p/interface` in `node_modules` vs the
linked `../optimystic/packages/db-p2p-storage-web/node_modules`. `Uint8ArrayList` generic
parameter changed between versions. This ticket touches no files in `reference-app-web`.

---

### 2. `cadre-host` integration test failures

**Command:** `cd packages/cadre-host && yarn test`

**Failing tests:** `src/auth/__tests__/trust-circle-integration.test.ts` — 2 tests:
- "issues → redeems → lists against the real control DB (listMembers path)"
- "removes a member from CadrePeer"

**Error excerpt:**
```
Error: Unsupported output encoding: utf8
 ❯ resolveOutputEncoder ../../../optimystic/packages/quereus-plugin-crypto/src/crypto.ts:129:9
 ❯ digest ../../../optimystic/packages/quereus-plugin-crypto/src/crypto.ts:324:56
 ❯ generateStampId ../cadre-core/src/control-database.ts:25:28
 ❯ ControlDatabase.insertAuthorityKey ../cadre-core/src/control-database.ts:489:21
 ❯ src/auth/__tests__/trust-circle-integration.test.ts:58:15
```

**Why pre-existing:** Failure is inside `quereus-plugin-crypto` and `control-database.ts`,
completely outside this ticket's diff (only `events.ts` and package.json scripts changed).
The failing code path is triggered by `insertAuthorityKey`, not any EventSource/SSE code.
370 other tests pass; only these 2 integration tests fail.
