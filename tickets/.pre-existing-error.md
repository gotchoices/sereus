# Pre-existing test failures seen during `review/retire-the-stream-reset-retry-fingerprint`

## Command

From the repo root:

```
yarn workspace @serfab/cadre-core test
```

Result: `Test Files 3 failed | 131 passed (134)`, `Tests 4 failed | 2175 passed | 1 skipped (2180)`.

One of the three failing files — `packages/cadre-core/test/control-start-storage-op-budget.spec.ts` — is already listed in `tickets/.pre-existing-known.md` against `warm-restart-into-declared-schema-diverges-from-declaration` (in `tickets/fix/`), so it is **not** re-reported here. The other two are not listed anywhere.

## Failing tests not yet tracked

**`packages/cadre-core/test/control-founding-consult-budget.spec.ts`** — both tests in the file:

- `control database founding, cohort consult and commit budget > stays within its consult and commit budgets from cold start through an idle reconcile pass`

  ```
  AssertionError: genesis issued only 7 cohort consults, far below (measured 14 consults over 3 blocks and 4 commits on 2026-09-15; …).
  This run's consults per block: default/cadrecontrol/OwnerKey×3, default/cadrecontrol/OwnerKey/index/_uniq_7.stampid×3, default/cadrecontrol/Revocation×1.
  Either the counter no longer sees this path (a renamed or bypassed CoordinatorRepo method, or a repo attributed to the wrong label), or the cost genuinely improved — in which case re-measure and TIGHTEN the budget.: expected 7 to be greater than 7
   ❯ expectWithinBudget test/control-founding-consult-budget.spec.ts:502:5
  ```

- `Revocation ledger marker, cohort consult budget > once the marker is filed, the membership reads, a control insert and a reconcile pass stop re-consulting`

  ```
  AssertionError: before the marker, queryRevokedStamps('CadrePeer'): per-call consults moved from [2, 2, 2, 2, 2, 2] (measured 2026-09-15) to [1, 1, 1, 1, 1, 1].
   ❯ expectPerCall test/control-founding-consult-budget.spec.ts:523:48
  ```

**`packages/cadre-core/test/strand-solo-write-budget.spec.ts`**:

- `solo strand write budget > stays within its operation budgets`

  ```
  AssertionError: solo select issued only 0 raw-storage operations, far below the measured 2 ops over 1 distinct blocks on 2026-09-14.
  Either this spec is no longer measuring the strand's real storage path (check the provider branch on strandId), or the cost genuinely improved — re-measure and TIGHTEN the budget rather than leaving the headroom.
  This run, calls/distinct-blocks by method: .: expected 0 to be greater than 1
   ❯ expectWithinBudget test/strand-solo-write-budget.spec.ts:256:4
  ```

## Why these are pre-existing

This ticket's entire diff — and the implement commit it reviews (`5e4494a`) — changes only comments, documentation prose and ticket files. No runtime code was touched by either. These three specs count Optimystic coordinator consults and raw-storage calls, a subsystem neither the implement pass nor this review pass goes near.

All four failures are the same shape and in the same direction: each trips the spec's **lower floor**, not its budget — the measured cost has gone *down* (14 → 7 consults, 2 → 1 consults per call, 2 → 0 raw-storage ops). That is consistent with the in-flight caching / block-floor work in `../optimystic` that `tickets/.pre-existing-known.md` records for 2026-09-17 (`cache-source.ts`, `block-floors.ts`, `tracker.ts`), which has since been committed there (`c0d8338c`, clean tree). The already-tracked `control-start-storage-op-budget.spec.ts` failure is a floor trip of exactly the same kind, so one root cause plausibly covers all three files — worth checking that first rather than triaging them separately.

Nothing was skipped, disabled or loosened.
