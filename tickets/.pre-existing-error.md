# Pre-existing test failure

Command: `yarn test` (root, which runs `yarn workspaces foreach -A run test`)
Package: `packages/integration-tests` (`@serfab/integration-tests`)

```
RUN  v4.1.8 C:/projects/sereus/packages/integration-tests
No test files found, exiting with code 1

⎯⎯⎯⎯⎯⎯ Unhandled Error ⎯⎯⎯⎯⎯⎯⎯
Error: Stale build detected: these tests run real compiled output.
  - @quereus/quereus: dist is stale — src was edited after the last build. Run in C:\projects\quereus: yarn workspace @quereus/quereus build
 ❯ assertCadreBuildFresh src/harness/build-freshness.ts:98:8
 ❯ Object.setup src/global-setup.ts:15:2
```

Why pre-existing / not mine: the linked sibling workspace `../quereus` (resolved via this repo's
root `resolutions`, `link:../quereus/packages/quereus`) has uncommitted source edits
(`packages/quereus/src/core/database-events.ts`, `packages/quereus/src/runtime/emit/alter-table.ts`,
plus a test file and `tickets/review/bug-alter-primary-key-leaves-stale-event-key.md`) newer than its
last `dist` build — that repo has its own in-flight ticket work happening concurrently, entirely
outside this repo and outside this ticket's diff (`0.2-debt-declared-dep-range-drift-gate`, which
only touches `package.json`, `scripts/check-dep-ranges.mjs`, `scripts/check-dep-ranges.test.mjs`,
and `docs/STATUS.md`). Rebuilding or otherwise touching `../quereus` here would step on that other
work in progress, so it was left alone.
