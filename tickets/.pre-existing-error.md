# Pre-existing failure: cadre-core suite cannot start (stale linked `@quereus/quereus` build)

Command run (from `C:\projects\sereus`):

```
yarn workspace @serfab/cadre-core test
```

Output:

```
 RUN  v4.1.8 C:/projects/sereus/packages/cadre-core

No test files found, exiting with code 1

⎯⎯⎯⎯⎯⎯ Unhandled Error ⎯⎯⎯⎯⎯⎯⎯
Error: Stale build detected: these tests run real compiled output.
  - @quereus/quereus: dist is stale — src was edited after the last build. Run in C:\projects\quereus: yarn workspace @quereus/quereus build
 ❯ assertBuildFresh ../../test-harness/build-freshness.ts:96:8
 ❯ Object.setup test/global-setup.ts:38:2
```

Root cause: `@quereus/quereus` is linked from the sibling repo `C:\projects\quereus` (`resolutions` in root `package.json`). That sibling currently has **uncommitted, non-compiling** edits in flight from unrelated work:

```
cd C:\projects\quereus && git status --short
 M packages/quereus/src/planner/building/coercion.ts
 M packages/quereus/src/types/cast-semantics.ts
?? packages/quereus/src/types/comparison-coercion.ts
```

Attempting `yarn workspace @quereus/quereus build` there (safe — `dist` is gitignored, no tracked files touched) fails on the sibling's own in-progress work:

```
src/runtime/emit/operand-comparator.ts(6,36): error TS2305: Module '"../../types/comparison-coercion.js"' has no exported member 'comparisonGroupIndices'.
```

This is the explicitly accepted failure mode documented in `test-harness/build-freshness.ts`'s own module comment on `checkLinkedTarget`: "a sibling repo's own automation editing mid-run aborts this suite, with a message naming exactly which sibling to rebuild." It is unrelated to this ticket's diff (`packages/cadre-core/test/cadre-node-control-node-options.spec.ts` new, `cadre-node-control-replication.spec.ts` trimmed, one doc-comment line in `cadre-node.ts`) — none of those touch quereus, comparison coercion, or cast semantics. `yarn workspace @serfab/cadre-core typecheck` (which does not go through the vitest global-setup guard) passes clean, including the new spec file.
