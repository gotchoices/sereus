# Pre-existing failure found during review of `debt-typecheck-vitest-configs`

Two unrelated red states surfaced while running this ticket's validation gates. Neither is
caused by the ticket's diff (six `tsconfig.typecheck.json` / two `package.json` `typecheck`
script edits plus `docs/STATUS.md` — no runtime source touched).

---

## 1. `yarn dep-check` gate is red (real defect, needs a fix)

**Command** (from repo root):

```
yarn dep-check          # → knip && yarn check:dep-ranges
```

**Exit code:** 1

**Failing finding:**

```
Unlisted dependencies (1)
@libp2p/peer-id  packages/reference-app-web/test/node-local-slots.spec.ts:16:38
```

`packages/reference-app-web/test/node-local-slots.spec.ts` imports `@libp2p/peer-id`, but
that package is not declared in `packages/reference-app-web/package.json` (neither
`dependencies` nor `devDependencies`) — it resolves only via hoisting. `knip.ts` downgrades
the dead-code rules (`files`, `exports`, `types`, …) to `warn`, but dependency rules stay at
`error`, so this one finding is what turns the gate red.

**Why it is pre-existing:** the spec file was added by `web-durable-node-local-stores-tests`
(commit `aca9c7d`, confirmed an ancestor of this ticket's implement commit `8d132fb`), and
`packages/reference-app-web` is not touched by this ticket's diff at all.

**Note:** `docs/STATUS.md` → "Dependency-check coverage" still claims *"The gate exits 0 with
no knip configuration hints"* — that claim is now stale and should be corrected by whoever
fixes the dependency.

---

## 2. `yarn workspace @serfab/cadre-core test` blocked by the stale-build guard (environmental, no defect)

**Command:**

```
yarn workspace @serfab/cadre-core test
```

**Exit code:** 1, during `globalSetup`, before any test executes:

```
Error: Stale build detected: these tests run real compiled output.
  - @quereus/quereus: dist is stale — src was edited after the last build. Run in C:\projects\quereus: yarn workspace @quereus/quereus build
 ❯ assertBuildFresh ../../test-harness/build-freshness.ts:96:8
 ❯ Object.setup test/global-setup.ts:38:2
```

This is **not** a test defect and needs no ticket — the guard is working as designed. The
sibling `../quereus` repo (linked in via root `resolutions`) has uncommitted in-flight edits
to `packages/quereus/src/runtime/emit/cast.ts`, `.../emit/subquery.ts` and
`.../types/cast-semantics.ts` that postdate its `dist`. Not rebuilt from here: that repo has
another agent's or a human's work in progress in it, and rebuilding it is out of this
ticket's scope.

Every other suite for the packages this ticket touches was run and is green:
`cadre-provider` 97/97, `strand-proto` 25/25, `cadre-cli` 99/99, `cadre-host` 465 passed /
4 skipped, `quereus-plugin-sereus` 68 passed / 1 todo. `yarn lint` exit 0; root
`yarn typecheck` exit 0.
