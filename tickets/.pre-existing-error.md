# Pre-existing failure report

Command (from `packages/integration-tests`):

```
yarn vitest run --reporter=verbose src/scenarios/push-wake-e2e.integration.ts
```

Failing tests (exact names, both in
`packages/integration-tests/src/scenarios/push-wake-e2e.integration.ts`):

- `E2E push-wake over the control network > wakes a hibernating member over a real direct control dial`
- `E2E push-wake over the control network > delivers a wake to a NAT'd receiver over a circuit-relay (signaling-first) dial`

Error excerpt (identical shape in both):

```
Error: insert failed: Some peers did not complete: <peerA>[block:...](in-flight)
cause=Transaction rejected by validators (2/2 rejected):
  <peerA>: membership-not-admitted:low-confidence-downsize;
  <peerB>: membership-not-admitted:low-confidence-downsize, ...
 ❯ OptimysticVirtualTable.update ../../../optimystic/packages/quereus-plugin-optimystic/src/optimystic-module.ts:1394:13
```

Why pre-existing: both tests fail at the first two-node-cohort control-DB commit
(`S.authorizePeer` / `L.authorizePeer`), before any code the
`membership-authorized-predicate-and-gates` ticket touched runs; the identical
failure reproduces at HEAD **before** this ticket's edits (verified 2026-07-27,
baseline run prior to any change). The signature is byte-identical to the root
cause already tracked in `blocked/control-db-convergence-optimystic-p2p`
(optimystic's member-side admission gate rejects a 2-node cohort against the
hardcoded `clusterSize: 3`) — these two tests are additional instances of that
known failure, not a new defect; they are just not yet listed in
`.pre-existing-known.md`. Scenarios 3 and 4 of the same file (no 2-node cohort
commit / a 3-node cohort) pass.

Also failing, same class (from `packages/integration-tests`, command
`yarn vitest run --reporter=verbose src/scenarios/control-cohort-auto-convergence.integration.ts src/scenarios/control-write-while-alone-convergence.integration.ts`):

- `control-cohort-auto-convergence.integration.ts > Control-cohort auto-convergence (no manual dial) > B converges on an owner-written CadrePeer row via in-node reconcile (production cold-start only)` — `Error: Timeout waiting for B observes the X CadrePeer row with no manual control dial after 45000ms`
- `control-write-while-alone-convergence.integration.ts > Control-DB write-while-alone re-replication > re-replicates an owner CadrePeer row written while alone, once the cohort forms` — `Error: Timeout waiting for B observes the X CadrePeer row written on A while alone after 30000ms`
- `control-write-while-alone-convergence.integration.ts > … > converges a DeviceToken self-registered while alone, once the cohort forms` — `Error: Timeout waiting for B resolves A DeviceToken re-replicated after cohort growth after 30000ms`

Why pre-existing: all three are two-node (A→B) control-DB convergence waits —
the exact failure mode of the tracked blocked slug — and the only edits the
`membership-authorized-predicate-and-gates` ticket made to these two files are
code comments (no executable change), so the failures cannot be caused by it.

Also failing, same class (from `packages/integration-tests`, command
`yarn vitest run src/scenarios/strand-creation.integration.ts src/scenarios/strand-formation-e2e.integration.ts src/scenarios/multi-party-sync.integration.ts src/scenarios/multi-party-workflows.integration.ts src/scenarios/websocket-chat.integration.ts src/scenarios/convergence-stress.integration.ts` —
10 failed / 19 passed, with 22 `membership-not-admitted:low-confidence-downsize`
occurrences in the output):

- `strand-formation-e2e.integration.ts > … > should form strand, start instances, and replicate data` (+ `should support multiple independent strands between same parties`)
- `multi-party-workflows.integration.ts > … Phase 1/2` — 4 tests (closed strand, open strand, interleaved writes, multi-round writes)
- `websocket-chat.integration.ts > … > should replicate a chat message over WebSocket`
- `convergence-stress.integration.ts > …` — 3 tests (burst, interleaved, disconnect/reconnect)

Why pre-existing: every failure is a TWO-party strand/control cluster commit
rejected by the same optimystic admission gate the blocked slug tracks
(`membership-not-admitted:low-confidence-downsize` — 2-node cluster vs
hardcoded `clusterSize: 3`); this ticket touched none of these files, and its
only production change (`CadreNode.listAuthorizedMembers`/`isAuthorizedMember`
+ a new verify helper) is not on any strand-formation or replication path. The
already-known entry `strand-membership-closed-strand-e2e` is the same class in
the same suite.
