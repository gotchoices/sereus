# Pre-existing test failure (found during implement of cadre-host-respawn-leaves-loan-unseedable)

## Command

From the repo root, after `yarn workspace @serfab/cadre-cli build` (the stale-build guard requires it):

```
yarn workspace @serfab/cadre-cli test
```

Result: `Test Files 1 failed | 15 passed (16)`, `Tests 2 failed | 234 passed (236)`.

## Failing tests

File: `packages/cadre-cli/test/one-shot-node.spec.ts`

- "one-shot commands against a real solo node > removes a seeded strand, and the row is gone from the control database afterwards"
- "one-shot commands against a real solo node > refuses a closed strand without --yes, exits 1, and leaves the membership key intact"

Excerpt:

```
AssertionError: stderr: Failed to remove strand: Failed to execute DDL: ALTER TABLE CadreControl.CadrePeer ALTER COLUMN UpdatedAt SET DATA TYPE int
Error: Module for table 'CadrePeer' does not support ALTER COLUMN
: expected 1 to be +0 // Object.is equality
 ❯ test/one-shot-node.spec.ts:285:55

AssertionError: expected 'Failed to remove strand: Failed to ex…' to contain '✗ Refusing to remove closed strand wi…'
+ Failed to remove strand: Failed to execute DDL: ALTER TABLE CadreControl.CadrePeer ALTER COLUMN UpdatedAt SET DATA TYPE int
+ Error: Module for table 'CadrePeer' does not support ALTER COLUMN
 ❯ test/one-shot-node.spec.ts:313:29
```

## Why it is pre-existing

The spawned `cadre strand remove` re-opens a control database the seed node already wrote (a warm restart), and fails in quereus's schema diff with exactly the "Group A" fingerprint of `tickets/blocked/warm-restart-into-declared-schema-diverges-from-declaration.md` (`ALTER TABLE CadreControl.CadrePeer ALTER COLUMN UpdatedAt SET DATA TYPE int`); this spec file is simply missing from that ticket's entries in `tickets/.pre-existing-known.md`. The ticket's changes to cadre-cli touch only the `start` command's startup-token write order and the `/seed` bearer-refusal logging, neither of which the one-shot `strand remove` path uses.
