# Pre-existing build/test failure: cadre-core digest 3-arg API skew

## Already ticketed — please do not duplicate or auto-fix

This failure is **already captured** by `tickets/plan/cadre-digest-3arg-api-migration.md`
(filed by the `sereus-cadre-schema-digest-api-migration` implement pass). It is a
**security-sensitive, cross-package migration** (TS-computed digests must byte-match SQL
`verify(digest(...))` constraints) and needs design + review — it should flow through the
plan ticket, **not** an inline triage auto-fix. If triage runs, prefer to defer to that
ticket rather than file a new backlog ticket.

## Commands run (from `packages/cadre-core`)

- `yarn typecheck`  → 24 errors
- `npx vitest run test/schema-verification.spec.ts` → 14/14 pass (this is the only part
  in scope for the implement ticket; it is fixed and green)

## Failure excerpt (`yarn typecheck`)

```
src/control-database.ts(25,55): error TS2554: Expected 1-3 arguments, but got 4.
src/control-database.ts(72,69): error TS2554: Expected 1-3 arguments, but got 4.
src/device-token.ts(38,43): error TS2554: Expected 1-3 arguments, but got 4.
src/peer-authorization.ts(17,43): error TS2554: Expected 1-3 arguments, but got 4.
src/peer-record.ts(56,43): error TS2554: Expected 1-3 arguments, but got 4.
src/seed-bootstrap.ts(415,59): error TS2554: Expected 1-3 arguments, but got 4.
src/seed-bootstrap.ts(635,61): error TS2554: Expected 1-3 arguments, but got 4.
src/strand-membership-writer.ts(50,55): error TS2554: Expected 1-3 arguments, but got 4.
src/strand-membership-writer.ts(71,59): error TS2554: Expected 1-3 arguments, but got 4.
... (+ 15 more in test/*.spec.ts)
```

The same 4-arg `digest(...)` pattern also lives in SQL DDL strings
(`control-schema.ts`, `quereus-plugin-sereus/src/strand-schema.ts`, `schemas/strand.qsql`,
`integration-tests/fixtures/simple-sapp.qsql`) and two integration scenarios.

## Why this is pre-existing (not caused by this ticket)

The implement ticket `0-sereus-cadre-schema-digest-api-migration` changed exactly one line
in `packages/cadre-core/src/schema-verification.ts` (4-arg → 3-arg `digest`). That edit
**removed** one of these errors and **added none** — `schema-verification.ts` is absent
from the error list above. All 24 remaining errors stem from optimystic's intentional
`digest` signature change (optimystic commits `8cea904`/`f10094c`), which predates and is
independent of this one-file edit; they fail identically at HEAD before the edit.

## Work-around used to complete the implement ticket

None needed — the scoped fix (`schema-verification.ts`) was validated in isolation via
`vitest run test/schema-verification.spec.ts` (14/14), which transpiles per-file and does
not depend on the broken modules. No failures were disabled or skipped.
