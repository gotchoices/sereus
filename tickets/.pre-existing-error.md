# Pre-existing test failures (found while working ticket debt-formation-one-budget-drives-both-sides)

## Command run

```
cd packages/cadre-core && yarn test
```
(also reproducible via `yarn workspace @serfab/cadre-core test` from repo root)

## Failing tests

`test/control-revocation-reissue.spec.ts`:
- `Revocation: owner-signed tombstone re-issue > an owner-signed counter bump lands, leaving the identity triple untouched`
- `Revocation: owner-signed tombstone re-issue > the counter moves strictly upward: equal and lower are refused, higher lands (ReissueOnly)`
- `Revocation: owner-signed tombstone re-issue > reissueRevocations bumps a whole batch in one transaction, and one stale counter rolls the batch back`
- `Revocation: owner-signed tombstone re-issue > a bump signed over the WRONG digest or by a non-owner is refused (AuthorizedReissue)`

`test/control-revocation-replay.spec.ts`:
- `Revocation: remove-then-replay resurrection is closed > Revocation: a tombstone is permanent — delete refused, and update may move only the counter (NoDelete / ReissueOnly / AuthorizedReissue)`

## Error excerpt

```
ConstraintError: UNIQUE constraint failed: Revocation.TableName, Revocation.StampId
 ❯ new ConstraintError ../../../quereus/packages/quereus/src/common/errors.ts:36:3
 ❯ processUpdateRow ../../../quereus/packages/quereus/src/runtime/emit/dml-executor.ts:1158:10
 ...
```
and
```
AssertionError: expected [Function] to throw error matching /CHECK constraint failed: (AuthorizedR…/ but got 'context.OwnerKey isn't a column'
```

## Why pre-existing

Ticket debt-formation-one-budget-drives-both-sides only touches
`packages/cadre-core/src/strand-formation-protocol.ts`,
`packages/cadre-core/src/strand-formation-manager.ts`, and adds new file
`packages/cadre-core/test/strand-formation-manager.spec.ts` — confirmed via
`git diff --stat 9f1dd22^ 74b7983` (the ticket's plan + both implement commits).
None of these touch the Revocation table, its DML/CHECK-constraint logic, or the
`control-revocation-*` spec files. The failures point into `../../quereus` (a sibling
workspace linked via `resolutions`, referenced for debug per AGENTS.md) — a
`UNIQUE constraint failed: Revocation.TableName, Revocation.StampId` on update, and a
`context.OwnerKey isn't a column` error where a `CHECK constraint failed` was expected —
both look like a quereus DML/constraint-evaluation regression unrelated to strand formation.
