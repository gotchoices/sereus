Command run (from `packages/integration-tests`):

```
yarn vitest run
```

Result: 5 test files failed / 24 passed, 6 tests failed / 126 passed. None of the 6 failures are
in `push-wake-e2e.integration.ts > ... learned by control-DB replication, not local seeding` —
the scenario this ticket (bug-control-db-stale-revision-not-retryable) targets passed in this
whole-file run, consistent with prior note that it "passes in whole-file runs" and is stable
alone-vs-together in different ways.

Failing tests:

1. `src/scenarios/cadre-host-bootstrap.integration.ts > cadre-host bootstrap > install + start →
   /api/status returns the live service snapshot` — `AssertionError: expected 500 to be 200`.
2. `src/scenarios/cadre-host-origin-guard.integration.ts > cadre-host origin guard > accepts
   loopback requests (default Host header)` — `AssertionError: expected 500 to be 200`.
3. `src/scenarios/cadre-host-origin-guard.integration.ts > cadre-host origin guard > accepts a
   matching loopback Origin header` — `AssertionError: expected 500 to be 200`.
4. `src/scenarios/cadre-host-trust-circle.integration.ts > cadre-host trust-circle > completes
   issue → list → redeem → list → remove cycle over the HTTP+service surface` —
   `AssertionError: expected [ { …(3) } ] to have a length of 2 but got 1`.
5. `src/scenarios/push-wake-e2e.integration.ts > E2E push-wake over the control network >
   delivers a wake to a NAT'd receiver over a circuit-relay (signaling-first) dial` —
   `UnsupportedListenAddressesError: ... UnexpectedEOFError: Unexpected EOF - stream closed while
   reading 0/1 bytes` inside `@libp2p/circuit-relay-v2` reservation-store during listen.
6. `src/scenarios/strand-formation-e2e.integration.ts > E2E Strand Formation > Phase 2: Strand
   instance lifecycle > should form a strand with three parties` — `Error: Timeout waiting for
   data replicates to Bob after 15000ms`.

Why believed pre-existing / not caused by this ticket's diff: the diff for
bug-control-db-stale-revision-not-retryable touches only
`../optimystic/packages/db-p2p/src/repo/cluster-coordinator.ts` and
`../optimystic/packages/db-p2p/src/repo/coordinator-repo.ts` (stale-revision rejection
classification inside the control-DB write-conflict path), plus two spec files. None of the 6
failures above are in that code path:
- #1-3 are HTTP-surface failures (`/api/status`, origin-guard header checks) in `cadre-host`,
  returning bare 500s — consistent with resource contention (many real libp2p nodes/ports
  spun up across 29 files in one process) rather than a logic regression.
- #4 is a membership-list-length race in `cadre-host-trust-circle`, unrelated subsystem.
- #5 is a circuit-relay transport listen failure (`UnexpectedEOFError` in
  `@libp2p/circuit-relay-v2`), a different sub-test of the same scenario file than the one this
  ticket touches; no `PartialCommitError` or `SyncRetryExhaustedError` involved.
- #6 is a replication timeout in an unrelated scenario (`strand-formation-e2e`), same general
  class as `bug-control-db-rx-record-never-converges-on-sender` (control-DB replication
  read-path non-convergence under load) but a distinct test/file, not exercised or touched by
  this ticket's change.
- `db-core` (1267 passing) and `db-p2p` (1436 passing / 41 pending) full unit suites are green
  after the diff, and the targeted scenario's own 20-run sample never showed any of these 6
  failure shapes — only `Timeout waiting for S resolves Rx's address record` (Shape B),
  `SyncRetryExhaustedError` (with/without a resulting `PartialCommitError`), and one prior
  unclassified failure. See `tickets/review/bug-control-db-stale-revision-not-retryable.md` for
  that scenario's own evidence.

Not run against a clean `master`/`main` checkout to double-confirm (would require stashing an
uncommitted, built, cross-linked `../optimystic` working tree and rebuilding downstream
consumers) — flagging via this file per process instead of chasing further in this ticket.
