## Pre-existing failure surfaced during `control-peer-row-refresh-invisible-to-third-node` (review handoff)

**Command:**

```
cd packages/integration-tests
npx vitest run src/scenarios/control-cohort-three-node-isolation.integration.ts --reporter=verbose
```

(monorepo: `packages/integration-tests`, i.e. `@serfab/integration-tests`)

**Failing test:** `src/scenarios/control-cohort-three-node-isolation.integration.ts > Control-cohort reconcile as sole connector (three nodes, no manual dial) > is load-bearing: without a reconcile pass B never reaches C, and one pass forms the link`

**Error excerpt:**

```
AssertionError: expected [] to include '12D3KooWBFQANHREyh3VekyJQ6LpGWymtRaZT…'
 ❯ src/scenarios/control-cohort-three-node-isolation.integration.ts:179:43
    177|    // transactor opened it inside that pass, the pass would have skipp…
    178|    // as already connected.
    179|    expect(dialsToC.openingPass()?.dialed).toContain(cPeerId);
```

The file's other test (`B automatically dials C once C's record replicates through A`) passed in the same run.

**Why this is pre-existing, not caused by this ticket's changes:** this ticket (`control-peer-row-refresh-invisible-to-third-node`) only edits comments — a `NOTE:`/header comment in `control-trio.ts`, `control-write-degraded-cohort-member.integration.ts`, and `control-divergent-repair-yardstick.integration.ts`, plus `tickets/.pre-existing-known.md` and a backlog ticket. `control-cohort-three-node-isolation.integration.ts` itself was not touched. The same test, on the same day, against the fix build (`../optimystic` `6e8efecd`), passed cleanly in an isolated run recorded at `tickets/.logs/control-peer-row-refresh.confirm-control-cohort-three-node-isolation.log` (`2 passed (2)`, "is load-bearing…" at 19513ms). This file is independently documented throughout `tickets/.pre-existing-known.md` as boot/dial-race sensitive (e.g. "control-cohort-three-node-isolation — flaky boot race per its own entry, needs repeat runs"). The assertion here (`dialsToC.openingPass()?.dialed` empty) is a different fingerprint than the boot-gate timeout this ticket closed out (`Timeout waiting for B resolves C's signed CadrePeer address record after 45000ms`) — it is about which reconcile pass the harness's `dialsToC` tracker attributes the B→C dial to, not about reading a stale address record.
