# Pre-existing test failure (not from `1-membership-authorized-surface-split`)

## Command

```
# from packages/cadre-host
yarn test --run release-key
```

## Failing tests

`packages/cadre-host/src/update/__tests__/release-key.test.ts`:

- `isPlaceholderReleaseKey > is true for the default all-zeros placeholder (no override)` — expected `true`, got `false` (release-key.test.ts:17)
- `isPlaceholderReleaseKey > honors an explicit env argument over process.env` — `isPlaceholderReleaseKey({})` expected `true`, got `false` (release-key.test.ts:27)

Error excerpt:

```
AssertionError: expected false to be true // Object.is equality
 ❯ src/update/__tests__/release-key.test.ts:17:39
```

## Why pre-existing

`isPlaceholderReleaseKey()` returns `false` because a real production signing key was
embedded into the source default (commits `b48b027` / `fb79894`), so the "all-zeros
placeholder" check no longer sees zeros. This subsystem (`update/release-key`) is entirely
outside the membership-surface-split diff, whose only `cadre-host` edit is two client methods
added to `authority/authority-node-client.ts`. Fails at HEAD independent of this ticket.

## Note

`orchestrator.test.ts > getStats > zero network counters` was reported in the implement
handoff as a third pre-existing failure; on this run it did not fail (appeared skipped). The
two release-key failures are the reproducible ones.
