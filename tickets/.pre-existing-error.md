# Pre-existing failure seen during `debt-stale-build-guard-remaining-suites-verify` (review stage)

## Command

```
yarn workspace @serfab/integration-tests test
```

(run from repo root, package `@serfab/integration-tests`)

## Failing test

`packages/integration-tests/src/scenarios/zz-scratch-delete-alone.integration.ts`
→ `SCRATCH delete-while-alone v2 > does a genuinely local-only removePeer reach a sibling that already has the row?`

Result line: `Test Files 1 failed | 36 passed (37)` / `Tests 1 failed | 191 passed | 1 expected fail (193)`, 39234 ms for the failing test.

Error excerpt:

```
SyncRetryExhaustedError: sync for collection default/CadrePeer exhausted 10 retries:
  stale revision: block t-2sG1DlnX0ep6EOXmI9NE8G0uTfdbr9DIgR3fn2vcs at rev 9, requested rev 9
 ❯ Collection.syncInternal ../../../optimystic/packages/db-core/src/collection/collection.ts:341:12
 ❯ Collection.updateAndSync ../../../optimystic/packages/db-core/src/collection/collection.ts:377:4
 ❯ Tree.sync ../../../optimystic/packages/db-core/src/collections/tree/tree.ts:120:4
 ❯ TransactionBridge.commitDirtyTreesLegacy ../../../optimystic/packages/quereus-plugin-optimystic/src/optimystic-adapter/txn-bridge.ts:402:9
```

## Why this is pre-existing

The failing file is not touched by this ticket — this ticket only added/edited
vitest global-setup and build-target spec files plus tsconfig/doc edits, and the
failure is inside `@optimystic/db-core`'s collection sync, reached through
`quereus-plugin-optimystic`'s transaction bridge. The file was last changed by
`f0a1a49 ticket(review): debt-cadrepeer-writes-validate-and-cover`, before this
ticket's implement commits.

## Extra context for triage

- The file's own header says: *"SCRATCH experiment (plan stage,
  control-delete-while-alone-tombstone). DELETE THIS FILE after the experiment
  settles. Not a regression test yet."* It is committed scratch, not a regression
  test — deleting it may be the correct disposition rather than fixing it.
- The suite is flaky run-to-run over real network: an earlier full run in the same
  session reported `3 failed | 34 passed (37)` with 2 failed tests and the same
  `stale revision … default/CadrePeer` signature; the run recorded above reported
  `1 failed | 36 passed (37)`. No test was skipped or loosened either time.
