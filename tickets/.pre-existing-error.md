# Pre-existing failure: cadre-core / cadre-cli test suites cannot start (stale + broken linked `quereus` build)

Found while implementing `5.3-feat-validation-key-enrollment`. Nothing in this repo's diff is involved.

## Commands run

From `packages/cadre-core`:

```
yarn vitest run test/validation-key-enrollment.spec.ts
```

## Failure

No test file even loads. The shared stale-build guard (`test-harness/build-freshness.ts`, wired
into `packages/cadre-core/test/global-setup.ts`) aborts the whole run:

```
Error: Stale build detected: these tests run real compiled output.
  - @quereus/quereus: dist is stale — src was edited after the last build.
    Run in C:\projects\quereus: yarn workspace @quereus/quereus build
 ❯ assertBuildFresh ../../test-harness/build-freshness.ts:96:8
 ❯ Object.setup test/global-setup.ts:38:2
```

Running the build it asks for then fails to compile:

```
$ (in C:\projects\quereus) yarn workspace @quereus/quereus build
src/func/builtins/mutation.ts(59,15): error TS2304: Cannot find name 'INTEGER_RETURN_NOT_NULL'.
```

## Why this is pre-existing (and not this repo's problem)

`C:\projects\quereus` is a separate sibling workspace, linked into Sereus via the root
`package.json` `resolutions`. It is at commit `bfa86e42` with **uncommitted, in-progress
edits** to exactly the files the error names:

```
 M packages/quereus/src/func/builtins/mutation.ts
 M packages/quereus/src/func/builtins/return-types.ts
 M packages/quereus/src/func/builtins/scalar.ts
 M packages/quereus/src/func/builtins/aggregate.ts
 M packages/quereus/src/func/builtins/conversion.ts
 M packages/quereus/src/func/builtins/builtin-window-functions.ts
```

That is a concurrent ticket mid-flight in another repo (its last commit message is
`ticket(implement): builtin-scalar-function-declared-return-types`). Those edits touched
`src` after the last `dist` build, which trips the freshness guard, and they do not yet
compile, so the guard's own remedy cannot be applied. Per the tess rules the working tree was
not sanitized and the sibling repo was not modified.

## Effect on this run

`yarn build` (exit 0) and `yarn lint` (exit 0) both pass at Sereus HEAD with the
`5.3-feat-validation-key-enrollment` diff applied. Only the vitest suites are blocked — no
cadre-core or cadre-cli test could be executed, including the new
`packages/cadre-core/test/validation-key-enrollment.spec.ts`. No test was skipped, disabled,
or loosened.

Unblocks as soon as the `quereus` edits compile and `yarn workspace @quereus/quereus build`
succeeds; re-run `yarn vitest run test/validation-key-enrollment.spec.ts` in
`packages/cadre-core` at that point.
