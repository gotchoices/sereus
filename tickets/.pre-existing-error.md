# Pre-existing (external) integration-suite failures — 2026-07-31

## Command

```
cd packages/integration-tests && npx vitest run --reporter=dot
```

Result: `Test Files 12 failed | 28 passed (40)`, `Tests 18 failed | 201 passed | 1 expected fail (220)`,
duration 934 s.

## Why these are not from this ticket

This ticket (`debt-strand-unpublish-sibling-convergence-e2e`, review stage) added one integration
scenario plus two additive options in `packages/integration-tests/src/harness/node-fixtures.ts`.
Its own scenario, `strand-unpublish-sibling-convergence.integration.ts`, **passes** and is not in
the failure list below.

**Root cause is outside this repo, and is a transient working-tree state, not a committed defect.**
The suite's stale-build guard refused to start, reporting `@optimystic/db-core` and
`@optimystic/db-p2p` dist older than src. Rebuilding both from `C:\projects\optimystic` cleared the
guard — but `git status` in that workspace shows an **uncommitted, in-flight change set** (a
concurrent agent run mid-ticket there):

```
 M packages/db-core/src/collection/collection.ts
 M packages/db-core/src/network/struct.ts
 M packages/db-core/src/transactor/network-transactor.ts
 M packages/db-core/src/transactor/transactor-source.ts
 M packages/db-p2p/src/repo/coordinator-repo.ts
 M packages/db-p2p/src/storage/storage-repo.ts
?? packages/db-p2p/test/coordinator-repo-unavailable.spec.ts
```

So the rebuild compiled half-finished transactor/collection/repo work into the dist this suite
links against. Every failure below is a **cross-node replication or convergence** assertion — the
exact surface those files own. Nothing in the failure set touches a sereus source file this repo
changed.

## Recommended triage action

**Do not chase these individually.** Re-run the suite once `C:\projects\optimystic` is clean and
committed, then treat any surviving failure as real. If failures persist against a committed
optimystic HEAD, the right home is a ticket in that sibling repo, not here.

## Failing tests

```
control-cohort-auto-convergence.integration.ts > B converges on an owner-written CadrePeer row via in-node reconcile (production cold-start only)
control-cohort-cold-start-retry.integration.ts > B recovers from a refused seed dial and converges on a later reconcile pass
control-cohort-edge-carries-data.integration.ts > a revision authored on C while B is fully isolated reaches B only across the reconcile-formed B→C connection
control-cohort-three-node-isolation.integration.ts > B automatically dials C once C's record replicates through A
control-cohort-three-node-isolation.integration.ts > is load-bearing: without a reconcile pass B never reaches C, and one pass forms the link
control-db-two-node-convergence.integration.ts > replicates an owner-written CadrePeer row from node A to node B over the live control network
control-write-while-alone-convergence.integration.ts > converges a DeviceToken self-registered while alone, once the cohort forms
control-write-while-alone-convergence.integration.ts > re-replicates an owner CadrePeer row written while alone, once the cohort forms
convergence-stress.integration.ts > should retain converged data after disconnect and reconnect
strand-addr-seed-convergence.integration.ts > joins a second node into the founder's strand from the RPC-resolved seed alone
strand-formation-e2e.integration.ts > should form strand, start instances, and replicate data
strand-membership-closed-strand-e2e.integration.ts > (all 5 tests)
websocket-chat.integration.ts > should replicate a chat message over WebSocket
zz-scratch-delete-alone.integration.ts > does a genuinely local-only removePeer reach a sibling that already has the row?
```

Sample error excerpt (`zz-scratch-delete-alone.integration.ts:99`) — representative of the whole
set, a row that never replicates:

```
- true
+ false

 ❯ src/scenarios/zz-scratch-delete-alone.integration.ts:99:38
     99|    expect(await B.isMember(xPeerId)).toBe(true);
```

Note: `zz-scratch-delete-alone.integration.ts` is itself a self-declared throwaway experiment
("SCRATCH experiment … DELETE THIS FILE after the experiment settles") belonging to
`plan/10-control-delete-while-alone-tombstone`. It is not a regression test and should not be
treated as one.

No test was skipped, disabled, or loosened.
