# Pre-existing failure report (2026-08-01, from control-revocation-drain-on-growth run 3)

## Command

```
cd packages/cadre-core && yarn test
```

Whole-suite result: 15 failed / 1357 passed / 1 skipped across 85 files. Of those, the 8
failures in `test/control-database-offline-peers.spec.ts` (plus its unhandled-rejection
noise) are **caused by this ticket's in-flight behavior change** (the spec at
`runRemoveWrite` ~line 316 asserts the old `pendingPeerWrites.get(...) === 'remove'`
contract) and are NOT reported here — the ticket's next run fixes that spec. This report
covers the remaining 7, which are plainly not this ticket's.

## Failing tests

`packages/cadre-core/test/control-revocation-reissue.spec.ts` (4):
- "an owner-signed counter bump lands, leaving the identity triple untouched"
- "the counter moves strictly upward: equal and lower are refused, higher lands (ReissueOnly)"
- "a bump signed over the WRONG digest or by a non-owner is refused (AuthorizedReissue)"
- "reissueRevocations bumps a whole batch in one transaction, and one stale counter rolls the batch back"

`packages/cadre-core/test/control-revocation-replay.spec.ts` (1):
- "Revocation: a tombstone is permanent — delete refused, and update may move only the counter (NoDelete / ReissueOnly / AuthorizedReissue)"

Error (first four identical; the fifth expects a `ReissueOnly`/`AuthorizedReissue` CHECK
refusal and gets this instead):

```
ConstraintError: UNIQUE constraint failed: Revocation.TableName, Revocation.StampId
 ❯ processUpdateRow ../../../quereus/packages/quereus/src/runtime/emit/dml-executor.ts:1158:10
```

i.e. the `update CadreControl.Revocation ... set ReissuedAt = ? where StampId = ?`
statement — which never changes the primary-key columns — now self-conflicts on the
table's UNIQUE(PK) inside quereus's update executor.

`packages/cadre-core/test/membership-connection-gater.spec.ts` (1) and
`packages/cadre-core/test/control-stream-authorization.spec.ts` (1):
- "denies a member whose StampId is retired in Revocation, still admitting its live sibling"

```
AssertionError: expected true to be false   // the retired member is still admitted
```

Same subsystem (the Revocation retired-stamp filter shipped by
`control-revocation-reissuable-tombstone`), so plausibly the same root as the five above.

## Why pre-existing

These specs import none of this run's edited files (working-tree edits touch
`src/cadre-node.ts`, `src/seed-bootstrap.ts`, and
`test/cadre-node-control-replication.spec.ts` only; the failing specs drive
`ControlDatabase` / schema constraints directly). The throw originates inside the linked
sibling workspace `../quereus`, which now sits at a freshly-released **v4.6.0**
(`f620aade`, clean tree) — these same specs were green when the prereq ticket
(`control-revocation-reissuable-tombstone-tests`, commit `049aba0`) landed them, so this
is sibling build/behavior drift (a quereus update-executor change breaking a
PK-preserving UPDATE), not anything this ticket's code paths touch. Confirmation step for
triage: run `npx vitest run test/control-revocation-reissue.spec.ts` on a clean sereus
checkout at HEAD against `../quereus` `f620aade` — it should fail identically with no
sereus working-tree changes present.
