# Pre-existing failure: `quereus-plugin-sereus` networked e2e (4 tests)

Observed during review of `debt-strand-breadth-counts-machines` (2026-08-03).

## Command

```
cd packages/quereus-plugin-sereus && yarn test
```

Result: `Test Files 1 failed | 7 passed (8)`, `Tests 4 failed | 73 passed | 1 todo (78)`.

## Failing tests

All in `packages/quereus-plugin-sereus/test/e2e/networked.e2e.spec.ts`:

- `connectToStrand (networked e2e) > replicates a single insert from peer A to peer B`
- `connectToStrand (networked e2e) > late-joining peer catches up to existing strand state`
- `connectToStrand (networked e2e) > peer A keeps serving reads after peer B shuts down`
- `strand sizes under the default breadth > commits a write on a 4-node strand after one holder stops`

## Excerpt

Three of the four die at strand bring-up, applying the `Strand` membership schema:

```
QuereusError: Failed to execute DDL: create table Strand.MemberPeer (...)
Error: Module 'optimystic' create failed for table 'MemberPeer':
  Failed to initialize Optimystic table: Missing block (PhPv0thaNNFIxCnteA0hbQPBD8JRAf1-HCpVU_UwISQ)
 ❯ OptimysticVirtualTable.doInitialize ../../../optimystic/packages/quereus-plugin-optimystic/src/optimystic-module.ts:316:13
 ❯ composeStrand src/compose-strand.ts:271:3
```

The fourth is a replication timeout rather than a bring-up error:

```
Error: Timeout waiting for late-joining peer B to catch up to 3 rows after 10000ms
 ❯ waitUntil test/e2e/networked.e2e.spec.ts:59:8
```

## Why pre-existing

This ticket's entire diff is prose: two sentences in `docs/architecture.md` / `docs/strands.md`
and one doc-comment paragraph in `packages/quereus-plugin-sereus/src/cluster-size.ts`.
`git diff -U0 -- packages/ | grep '^+' | grep -vE '^\+\s*\*|^\+\s*/\*|^\+\s*//'` returns nothing —
there is not one added executable line in the tree. The failures are p2p block fetches across
real libp2p peers (`Missing block` during `OptimysticVirtualTable.doInitialize`, and a
replication catch-up timeout), which no comment text can reach.

Not listed in `tickets/.pre-existing-known.md`. The `Missing block` shape resembles the tracked
`control-db-cross-node-convergence-halted` class (block committed but read as absent), but that
class's entries are all `packages/integration-tests` control-network scenarios — this is a
strand-network suite in a different package, so it is reported rather than folded in.
