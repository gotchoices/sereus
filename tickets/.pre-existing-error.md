# Pre-existing test failure (flagged by ticket 2-rename-strand-authority-to-manager)

## Commands run (from repo root)

- `yarn workspace @serfab/integration-tests run test strand-membership-closed-strand-e2e`
- `yarn workspace @serfab/quereus-plugin-sereus run test networked.e2e`

## Failing tests

1. `packages/integration-tests/src/scenarios/strand-membership-closed-strand-e2e.integration.ts`
   > Closed-strand membership lifecycle (real two-node strand) > founds a closed strand, admits a second member, and gates writes by membership
2. `packages/quereus-plugin-sereus/test/e2e/networked.e2e.spec.ts` (4 failing cases)

## Error excerpt (identical shape across all failures)

```
Error: Some peers did not complete: <peerId>[block:<id>](in-flight)
  cause=Transaction rejected by validators (2/2 rejected):
    <peerId>: membership-not-admitted:low-confidence-downsize;
    <peerId>: membership-not-admitted:low-confidence-downsize
 ❯ ClusterCoordinator.executeTransaction ../../../optimystic/packages/db-p2p/src/repo/cluster-coordinator.ts:337:10
 ❯ NetworkTransactor.pend ../../../optimystic/packages/db-core/src/transactor/network-transactor.ts:502:22
```

## Why this is pre-existing (not caused by this ticket)

- This ticket is a pure symbol rename of the strand-membership RBAC role
  (`Strand.Authority` table + `ManagerKey`/`ManagerSignature` context params + the
  `addManager`/`removeManager`/`addMemberByManager` API). It changes no networking,
  consensus, or cluster-membership code.
- The rejection (`membership-not-admitted:low-confidence-downsize`) is emitted by the
  **linked `optimystic` workspace** (`db-p2p/src/repo/cluster-coordinator.ts`), which
  governs optimystic *cluster/consensus* membership — a different layer from the
  strand `Manager` RBAC role. That validator does not read the renamed table.
- `networked.e2e.spec.ts` does not touch the strand Authority/Manager role at all yet
  fails with the *identical* validator rejection — so the failure is a systemic
  real-network p2p issue in this environment, independent of the rename.
- Same root-cause subsystem as the already-tracked
  `control-db-convergence-optimystic-p2p` (listed in `.pre-existing-known.md`,
  status: blocked): two-node optimystic p2p transaction convergence rejected by
  validators. These two failing tests are almost certainly the same root cause and
  can likely be attached to that blocked slug rather than triaged fresh.
- Deterministic across two runs (not flaky). All in-engine sereus suites pass:
  cadre-core strand suites (60 tests), quereus-plugin strand-schema drift + e2e
  (21 tests), `yarn lint`, and build/typecheck for cadre-core, quereus-plugin-sereus,
  and integration-tests.

No test was skipped, disabled, or loosened.
