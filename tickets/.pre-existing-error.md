# Pre-existing failures seen during `review/control-write-hears-zero-approvals-from-healthy-trio` (2026-09-17)

## Command

`yarn workspace @serfab/cadre-core test` (from the repo root), vitest 4.1.8. Result: **3 files failed / 131 passed (134); 4 tests failed / 2173 passed / 1 skipped.**

## Failing tests

All four failures are the same shape: a cost-budget spec's **lower floor**, not its budget. Each spec records a measured cost from 2026-09-14/15 and asserts the current run is still within roughly half of it, so that a cost which silently drops to zero (instrumentation no longer seeing the path) reddens instead of passing. Every one of them now measures a *lower* cost than its baseline.

- `packages/cadre-core/test/control-founding-consult-budget.spec.ts > control database founding, cohort consult and commit budget > stays within its consult and commit budgets from cold start through an idle reconcile pass`
  `AssertionError: genesis issued only 7 cohort consults, far below (measured 14 consults over 3 blocks and 4 commits on 2026-09-15 …): expected 7 to be greater than 7`
- `packages/cadre-core/test/control-founding-consult-budget.spec.ts > Revocation ledger marker, cohort consult budget > once the marker is filed, the membership reads, a control insert and a reconcile pass stop re-consulting`
  `AssertionError: before the marker, queryRevokedStamps('CadrePeer'): per-call consults moved from [2, 2, 2, 2, 2, 2] (measured 2026-09-15) to [1, 1, 1, 1, 1, 1]`
- `packages/cadre-core/test/strand-solo-write-budget.spec.ts > solo strand write budget > stays within its operation budgets`
  `AssertionError: solo select issued only 0 raw-storage operations, far below the measured 2 ops over 1 distinct blocks on 2026-09-14 … This run, calls/distinct-blocks by method: .: expected 0 to be greater than 1`
- `packages/cadre-core/test/control-start-storage-op-budget.spec.ts > control database start, raw-storage operation budget > stays within its operation budget on a cold start and on a warm restart`
  `AssertionError: warm start issued only 13 raw-storage operations, far below the measured 44 ops over 22 distinct blocks on 2026-09-14 … getMetadata 3/3, listRevisions 3/3, getMaterializedBlock 3/3, listPendingTransactions 3/3, listBlockIds 1/0: expected 13 to be greater than 22`

## Already tracked — do not re-triage

The **fourth** one (`control-start-storage-op-budget.spec.ts`) is already listed in `tickets/.pre-existing-known.md` under Open, owned by `fix/warm-restart-into-declared-schema-diverges-from-declaration`, with this exact fingerprint ("13 ops over 3 blocks, which trips the spec's LOWER floor"). It is repeated above only so the run's full result is on record.

The other three are **not** listed anywhere in that file. `strand-solo-write-budget.spec.ts` has a 2026-08-17 "Resolved (entry removed)" note there which anticipates a different regression ("if either spec reds again near ~2000 ops, that is the cache leaving the path") — this failure is the opposite direction (0 ops), so that note does not cover it.

## Why these are pre-existing

This review ticket's diff is comments, `docs/architecture.md` prose, one added unit-test case in `packages/cadre-core/test/control-write-retry.spec.ts`, and one corrected comment in `packages/cadre-core/test/control-formation-seat-budget.spec.ts`. No runtime code changed in this ticket or the implement commit it reviews (`70dcf90`). The suite run that produced these four failures was executed at HEAD **before** any edit in this session, and none of the four specs is in the diff's file set. They measure raw-storage and cohort-consult call counts against `@optimystic/db-p2p`, whose `dist/` in `../optimystic` is currently older than its `src/` — the likeliest cause is an upstream cost reduction landing after the 2026-09-14/15 baselines were taken, in which case the fix is to re-measure and tighten each floor, not to widen it.

No test was skipped, disabled, or loosened.
