## Pre-existing failure: `../optimystic` sibling repo has an uncommitted, broken `db-core` build

Command run (from `packages/cadre-core`):

```
yarn vitest run test/node-local-snapshot.spec.ts test/trusted-owner-store.spec.ts test/bootstrap-peer-store.spec.ts
```

Result: vitest never reaches any test — `global-setup.ts`'s `assertBuildFresh` guard aborts the
whole run up front:

```
Error: Stale build detected: these tests run real compiled output.
  - @optimystic/db-core: dist is stale — src was edited after the last build. Run in C:\projects\optimystic: yarn workspace @optimystic/db-core build
  - @optimystic/db-p2p: dist is stale — src was edited after the last build. Run in C:\projects\optimystic: yarn workspace @optimystic/db-p2p build
```

Ran the suggested build in `../optimystic` to clear it; it fails to compile:

```
src/transactor/network-transactor.ts(562,21): error TS2304: Cannot find name 'highestStaleAt'.
src/transactor/network-transactor.ts(687,21): error TS2304: Cannot find name 'highestStaleAt'.
```

Why this is not mine: `../optimystic` is a sibling repository (linked via `resolutions` in the
root `package.json`, per `AGENTS.md`), not part of this ticket's diff — this ticket only touches
`packages/cadre-core/src/{trusted-owner-store-file,bootstrap-peer-store-file,fs-atomic,file-durable-slot}.ts`
and `packages/cadre-core/test/node-local-snapshot.spec.ts`, none of which reference
`@optimystic/*`. `git status` in `../optimystic` shows uncommitted changes to
`packages/db-core/src/transactor/network-transactor.ts` and four other files (HEAD `c4c7bc8`,
"ticket(plan): stale-failure-carries-coordinator-revision") — another agent/session appears to be
mid-refactor there, in-flight and currently non-compiling. Not touched or reverted.

Impact: blocks running `yarn vitest` for `packages/cadre-core` (and anything else whose
`global-setup.ts` lists `@optimystic/db-core` / `db-p2p` as a linked target) entirely, not just
one test. Verified this ticket's own changes instead via `yarn tsc --noEmit -p packages/cadre-core`
(clean) and manual review of the new concurrency test's control flow.
