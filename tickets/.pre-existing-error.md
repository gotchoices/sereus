# Pre-existing test failure

Command: `yarn workspace @serfab/cadre-core test` (i.e. `yarn test` from `packages/cadre-core`)

Failing test: `test/control-formation-invite.spec.ts > control formation invite (consent path:
FormationInvite + FormationUsage) > consent-branch narrowing (unbound-only, open/keyless,
once-ever) > an unbound invite still seats one open, keyless strand per use, each with a distinct
id`

```
Error: Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 ❯ test/control-formation-invite.spec.ts:630:5
```

Full run: 1 failed | 1256 passed | 1 skipped (1258), 80/81 files passed.

Why pre-existing / not mine: this ticket (`14.1-cadre-announce-addrs-upstream`) only touches
`network.announceAddrs` — a startup warning in `CadreNode.start()`, a doc comment in
`packages/cadre-core/src/types.ts`, and comments in `example.cadre.yaml`/`docs/architecture.md`.
Nothing in the diff touches `FormationInvite`/`FormationUsage` or consent-branch narrowing. Ran
the same test alone (`yarn vitest run test/control-formation-invite.spec.ts -t "an unbound invite
still seats one open"`) and it passed in 2.05s — it only fails under the full 81-file parallel
run, where it hits the default 5000ms `it()` timeout under contention (the whole run took 120s,
mostly transform/import). Reads as a timing flake under load, not a logic defect introduced here.
