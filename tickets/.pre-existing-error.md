## Pre-existing failure: cadre-host orchestrator getStats timeout

Command: `yarn test` (root, runs all workspaces) — fails inside `packages/cadre-host` workspace.
Isolated repro: `cd packages/cadre-host && yarn vitest run src/__tests__/orchestrator.test.ts -t "returns plausible numbers with zero network counters"`

Failing test: `packages/cadre-host/src/__tests__/orchestrator.test.ts` →
`HostProcessOrchestrator.getStats > returns plausible numbers with zero network counters`

```
Error: Test timed out in 5000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 ❯ src/__tests__/orchestrator.test.ts:257:3
```

Why pre-existing: `cadre-host` has zero `@optimystic/*` or `@quereus/quereus` dependency (direct or
transitive via its own `package.json`) — nothing in this ticket's diff touches code this package
imports. Test passes reliably in isolation (3.4s, confirmed twice across two separate agent runs on
2026-07-29) but times out under full-suite load (5s timeout, spawns a real child process) — reads as
a timeout-margin flake in the test itself, not a regression tied to any specific change.
