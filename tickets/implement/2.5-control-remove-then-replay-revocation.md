----
description: Make removing an admin key or a party node permanent — today the original approval that let them in still works afterwards, so anyone who kept a copy can put them straight back.
prereq:
files: schemas/control.qsql + packages/cadre-core/src/control-schema.ts (Phase 1 DONE — must stay byte-identical, control-schema-drift.spec.ts), packages/cadre-core/src/control-authorization.ts (DONE), packages/cadre-core/src/seed-bootstrap.ts (removePeer), packages/cadre-core/src/control-database.ts, packages/cadre-core/src/cadre-node.ts (listAuthorizedMembers), packages/cadre-core/test/control-ownerkey-self-authorization.spec.ts, packages/cadre-core/test/control-authorization-domain-separation.spec.ts, packages/cadre-core/test/seed-bootstrap.spec.ts
difficulty: hard
----

<!-- resume-note -->
**A prior agent run was cut off by a soft token-budget limit after completing Phase 1 only.
The working tree is MID-IMPLEMENTATION and currently INCONSISTENT: the schema now REQUIRES a
`Revocation` tombstone on every `OwnerKey` / `CadrePeer` delete, but no writer or test has been
updated yet — `SeedBootstrapService.removePeer` and every existing raw-delete test WILL FAIL
`RevocationRecorded` until Phases 2 and 4 below land. Do not run the test suite expecting green
before those phases. No tests, lint, or typecheck were run by the prior agent.**

What already landed (verify by reading, don't redo):

- `schemas/control.qsql`: `NotRevoked` + `RevocationRecorded` constraints added to `OwnerKey`
  (right after its StampId column comment) and to `CadrePeer` (right before `AuthorizedInsert`);
  new `Revocation` table appended after `FormationUsage` (with `Immutable`, `RowIsGone`, and the
  two NOTE tripwires — convergence ordering, unbounded growth — in its header comment); the
  `StampId` column comments on both guarded tables rewritten to describe the retirement scheme.
- `packages/cadre-core/src/control-schema.ts`: the same edits mirrored into `CONTROL_SCHEMA`.
  Backticks inside the template literal are escaped (backslash + backtick) so the RUNTIME STRING
  is byte-identical to the .qsql file — the drift spec compares evaluated content, and the raw TS
  source necessarily differs by those escapes. Verify with `control-schema-drift.spec.ts` once
  the suite can run.
- `packages/cadre-core/src/control-authorization.ts`: `'Revocation'` appended (last, schema
  order) to `CONTROL_TABLES`, with a doc-comment note that its derived
  `'CadreControl.Revocation'` domain tag is unused (no signature minted for this table).

Remaining work = Phases 2–5 of the TODO below (Phase 1 done). The original ticket body follows
unchanged in substance; all its analysis, prototype findings, and test specifications still apply.
<!-- /resume-note -->

# Retire an approval's one-off stamp when the row it admitted is removed

## The bug, reproduced

Two control tables gate who is in the party: `CadreControl.OwnerKey` (keys allowed to make
control changes) and `CadreControl.CadrePeer` (nodes that are members of the cadre). Adding a
row to either needs an owner signature over `(row key, StampId)`, where `StampId` is a random
one-off value stored in a `unique` column.

`unique` only constrains rows that currently exist. Deleting the row frees its stamp, and the
original signature — which never expires — verifies again. Both cases were re-confirmed against
a real (optimystic-backed) control database during this fix pass:

```
OwnerKey : founder enrolls K -> founder removes K (properly signed) -> replay the ORIGINAL
           enrollment insert verbatim  =>  K is an owner again
CadrePeer: founder admits P  -> founder removes P (properly signed) -> replay the ORIGINAL
           admission insert verbatim   =>  P is a member again
```

For `CadrePeer` the approval is *stored on the row itself* (`VouchOwner` / `VouchSig`), so the
peer being removed can read and keep it while still a member. For both tables, the approval also
rides along with the write to every other node in the party. The replayer need not be the removed
party — any writer works, which is the attacker position the rest of the control-plane
authorization work assumes (an already-admitted cadre peer).

## The fix: an append-only `Revocation` table

Retire the stamp instead of letting it free up. A new table records `(TableName, StampId)` pairs
whose row has been removed; the guarded tables refuse an insert naming a retired stamp, and
refuse a delete that does not retire one.

A legitimate re-add is unaffected: `SeedBootstrapService.insertCadrePeerRow` and
`ControlDatabase.insertOwnerKey` both mint a **fresh** `StampId` per insert
(`control-database.ts:generateStampId`, 32 bytes of CSPRNG output) and sign over it, so
re-admitting a removed peer just works with a new approval.

This shape was prototyped end-to-end during the fix pass — both against a bare in-memory
Quereus `Database` and against a real `CadreNode` control database — and all of the following
hold:

- Quereus accepts the forward reference (`OwnerKey` referencing `Revocation`, declared later).
- `check on delete` and `check on insert` both auto-defer (they carry subqueries), so the
  delete and its sibling tombstone insert see each other at commit **in either statement order**.
- Delete without a tombstone is refused by name; tombstoning a live row is refused by name;
  clearing or re-pointing a tombstone is refused by name.
- Replaying the original add after removal is refused by name; a re-add with a fresh stamp
  succeeds.

The schema delta described by the original ticket is now LANDED in both schema files (see
resume-note). `Revocation` needs no `with context (...)` clause — none of its predicates reads
context (verified in the prototype).

### Writer change (Phase 2, outstanding)

`SeedBootstrapService.removePeer` (`seed-bootstrap.ts:~445`) is the only production deleter of a
guarded row — there is no production `OwnerKey` deleter at all (only tests). Its single
`delete from CadreControl.CadrePeer` must become a `beginTransaction` / `commit` pair holding the
delete plus `insert into CadreControl.Revocation (TableName, StampId) values ('CadrePeer', <stampId>)`.
It already reads the row's current `StampId` before signing (`queryCadrePeerStampId`), so the
value is in hand. Copy the rollback-swallow-and-rethrow shape from
`ControlDatabase.redeemInvitation` (`control-database.ts:~770-798`) rather than inventing a
second one.

`SeedBootstrapService.deleteDeviceToken` is untouched — `DeviceToken` has no stamp at all and is
covered by `bug-devicetoken-authority-antireplay`.

### Comment corrections still outstanding (Phase 2)

- `seed-bootstrap.ts` `insertCadrePeerRow` (~lines 336-341): the `NOTE:` block starting
  "StampId uniqueness only blocks insert-replay while the row ... is still present" describes
  this exact hole and defers it to the connection gater. Delete it; the hole is now closed at
  the schema.
- `peer-authorization.ts` doc comments on `cadrePeerVoucherDigest` / `cadrePeerRemoveDigest`
  claim single-use anti-replay via stamp uniqueness ("cannot be replayed (the StampId is
  unique)" / "binding the live row's StampId also invalidates any captured remove after a
  delete+reinsert (the nonce rotates)") — make them point at `Revocation` retirement instead.

Do **not** touch the `ValidationKey` / `Strand` / `FormationInvite` stamp comments: those tables
are handled by `bug-control-validationkey-strand-unauthorized-delete`.

## Known limits — already recorded as tripwires (Phase 1, done)

Both NOTEs sit on the `Revocation` table header comment in the schema (both copies):

- **Convergence ordering.** Per-transaction CHECKs against locally visible rows: a node that has
  not yet converged on a `Revocation` row still accepts the replayed add; rows coexist after
  merge. The read-side voucher check (Phase 3) is the mitigation.
- **Unbounded growth.** `Revocation` only ever grows; fine at cadre scale.

List both in the eventual review handoff's `## Review findings` as parked tripwires (findings is
the index; the NOTEs are the home).

## Read-side second layer (in scope, Phase 3)

Membership decisions should not depend on a write-time CHECK having been evaluated by the right
node. `CadreNode.listAuthorizedMembers` (`cadre-node.ts:~2857`) already re-verifies each
`CadrePeer` row's stored voucher against the node-local trusted-owner anchor
(`hasAnchoredVoucher`); extend it to also drop a row whose `StampId` appears in `Revocation`.
That makes a resurrected row inert for the reader even if it lost the write-time race, and it is
a cheap set lookup over a table the node already has. Needs a `ControlDatabase` read helper
(e.g. `queryRevokedStamps(table): Promise<Set<string>>`). Note `isAuthorizedMember` delegates to
`listAuthorizedMembers`, so one change covers both gates.

## TODO

Phase 1 — schema — **DONE** (see resume-note; verify via `control-schema-drift.spec.ts` when
the suite runs)

Phase 2 — writers

- Wrap `SeedBootstrapService.removePeer`'s delete and its `Revocation` insert in one explicit
  transaction on `this.controlDatabase.getDatabase()`, mirroring `redeemInvitation`'s
  rollback handling.
- Strip the now-false `NOTE:` in `insertCadrePeerRow` and fix the `peer-authorization.ts`
  doc comments (details above).

Phase 3 — read-side

- Add a `ControlDatabase` helper that reads the retired stamps for a table.
- Filter `CadreNode.listAuthorizedMembers` on it, next to the existing voucher check
  (`hasAnchoredVoucher`).

Phase 4 — tests

- New spec (suggested `packages/cadre-core/test/control-revocation-replay.spec.ts`), real
  `CadreNode` per test as `control-ownerkey-self-authorization.spec.ts` does (copy its
  `bootFreshParty` / `inTransaction` / `expectConstraintFailure` shapes; assert by constraint
  NAME):
  - `OwnerKey`: enroll -> signed remove (with tombstone) -> replay the original enrollment insert
    verbatim is refused by `NotRevoked`.
  - `CadrePeer`: admit -> signed remove (with tombstone) -> replay the original admission insert
    verbatim is refused by `NotRevoked`.
  - Both tables: a delete with no accompanying tombstone is refused by `RevocationRecorded`.
  - Both tables: a re-add with a FRESH stamp and a fresh signature succeeds (the fix must not
    make removal an identity-level ban).
  - `Revocation`: tombstoning a live row is refused by `RowIsGone`; deleting or updating a
    tombstone is refused by `Immutable`.
  - `listAuthorizedMembers` omits a peer whose stamp is retired even when the row is present.
- Update the existing raw deleters, which now fail `RevocationRecorded`:
  - `control-ownerkey-self-authorization.spec.ts` — `rawDeleteOwnerKey` (used by ~7 tests):
    fold the tombstone insert into the helper, wrapped in its existing `inTransaction`; the
    helper must read the row's StampId (via the existing `stampIdOf`) before deleting. Take
    care that the tests asserting a *rejected* delete still trip the constraint they name —
    a tombstone inserted alongside a REJECTED delete will itself fail `RowIsGone` (the row is
    still live at commit), so those tests may need `RowIsGone` added to their accepted
    constraint alternatives, or the helper split into signed/unsigned variants.
  - `control-authorization-domain-separation.spec.ts` — the genuine-signature proof deletes:
    the `OwnerKey` delete (~line 248) in "OwnerKey removal approval replayed as a CadrePeer
    delete" needs a tombstone; the REJECTED `CadrePeer` delete (~line 236) in the same test
    should still fail `AuthorizedDelete` (check whether `RevocationRecorded` now co-reports —
    the deferred queue does not promise which constraint reports first, so the assertion may
    need both names). The `DeviceToken` / `FormationInvite` deletes are unaffected (no
    Revocation coverage).
- `packages/cadre-core/test/seed-bootstrap.spec.ts` "authorizePeer / removePeer — round-trip"
  (~line 1015) exercises the real `removePeer` — confirm it still passes and add an assertion
  that the `Revocation` row landed (select TableName='CadrePeer' + the removed row's
  pre-delete StampId, which the test must capture before calling `removePeer`).

Phase 5 — validate

- From `packages/cadre-core`: `yarn vitest run 2>&1 | tee /tmp/cadre-core.log`, plus `yarn lint`
  and the typecheck at the repo root. Stream output; do not silently redirect.
- Then move this ticket to review/ with an honest handoff (tripwires listed for
  `## Review findings`).
