---
description: A machine lent to someone else's group stays dead after a crash or reboot; make the lending computer notice and start it up again on its own, and give up loudly instead of retrying forever.
prereq: 16-donated-node-respawn-core
files: packages/cadre-host/src/donation/donation-supervisor.ts (new), packages/cadre-host/src/donation/index.ts, packages/cadre-host/src/bin/host.ts, packages/cadre-host/src/donation/__tests__/, docs/cadre-host.md
difficulty: medium
---

# Supervise donated nodes and respawn the dead ones

Split out of the original `16-donated-node-respawn` ticket, which was decomposed
under a budget limit before any code landed. The prereq ticket
`donated-node-respawn-core` persists the spawn inputs, fixes the orchestrator's
stale-handle leak, and adds `DonationService.respawn(id)`. This ticket adds the
component that decides *when* to call it, plus the give-up path.

## Why nothing respawns today (verified trace against current `master`)

`HostProcessOrchestrator.init()` marks non-surviving children dead and exposes
them via `listDeadHandles()` (no production caller — the core ticket deletes it);
`bin/host.ts` re-spawns only the host's own owner node; `/api/nodes/:id/{start,restart}`
refuses non-owner ids with `not_implemented`; and `reapStaleAwaitingSeed` only
*terminates*. So a crashed, OOM-killed, or reboot-killed donated node stays dead
with its record still reading `seeded`.

## What to build

### `DonationSupervisor` (new file, `donation/donation-supervisor.ts`)

Owns the invariant *a non-terminal donation is expected to be running*.
Construction takes the `DonationService`, the `DonationStore`, the orchestrator,
and an injectable `now` (for tests).

```ts
export class DonationSupervisor {
  constructor(opts: DonationSupervisorOptions);
  /** One reconcile pass. Returns the donation ids it respawned. */
  reconcile(): Promise<string[]>;
  /** Startup sweep + interval timer + orchestrator state-change subscription. */
  start(): void;
  stop(): void;
}
```

`reconcile()` walks `store.list()`, considers only `awaiting_seed` and `seeded`
(`provisioning` is a provision in flight; `error` and `terminated` are terminal),
and for each with a `dockerId` checks `orchestrator.isRunning(dockerId)` — that
method is on the shared `Orchestrator` interface
(`packages/cadre-provider/src/service/orchestrator.ts:99`), so no new orchestrator
surface is needed. Not running → `DonationService.respawn(id)`, subject to the
backoff below.

Three triggers, one code path:

- **host startup** — one `reconcile()` after `orchestrator.init()`, alongside the
  existing `reapStale()` call in `bin/host.ts` (`:312-319`);
- **exit signal** — `orchestrator.onStateChange`; when a non-owner handle reports
  `status: 'stopped'`, run `reconcile()`. Cheap — do not try to map the handle
  back to a donation inside the listener;
- **periodic sweep** — its own timer, `unref()`d like `reapTimer`; catches the
  cases no exit event covers (host was down when the child died).

### Backoff and giving up

Module constants next to `DONATION_REAP_SWEEP_MS` (`donation-service.ts:29`):

- `DONATION_RESPAWN_BACKOFF_BASE_MS = 5_000`, doubling per attempt
- `DONATION_RESPAWN_BACKOFF_MAX_MS = 5 * 60_000`
- `DONATION_RESPAWN_MAX_ATTEMPTS = 5`
- `DONATION_RESPAWN_HEALTHY_MS = 10 * 60_000`
- `DONATION_RESPAWN_SWEEP_MS = 60_000`

A candidate is skipped until `now - lastAttemptAt >= min(base * 2^attempts, max)`.
`attempts` resets to 0 when a reconcile pass finds the node running **and**
`now - lastAttemptAt > DONATION_RESPAWN_HEALTHY_MS` — i.e. the last respawn
produced a node that stayed up, so the next crash starts from a fresh budget.

After `DONATION_RESPAWN_MAX_ATTEMPTS` consecutive attempts the host gives up:
stop the child but **do not** `removeContainer` (that deletes the workdir, and
with it the identity key the borrower's group approved), and set `status: 'error'`
with an `error` string naming the attempt count and the last failure. `error` is
outside `LIVE_STATUSES` (`donation-store.ts:16`), so the grant quota frees and the
borrower can provision a fresh node; `terminate()` still works on an `error`
record to reclaim the workdir.

Leave a `NOTE:` beside the give-up branch: an `error`-after-give-up record keeps
its workdir forever unless someone calls `terminate`. If stale `error` workdirs
ever pile up, extend the reap sweep to them. Conditional, not work for this
ticket.

### Wiring

Export `DonationSupervisor` and the five constants from `donation/index.ts`.
In `bin/host.ts`: construct after `donationService` (`:302-306`), `start()` it
beside the reap timer, `stop()` it in the shutdown block next to
`clearInterval(reapTimer)` (`:424`).

## TODO

- New `donation/donation-supervisor.ts` with `reconcile` / `start` / `stop`, the
  five constants, exponential backoff, healthy-reset, and the give-up-to-`error`
  path.
- Export from `donation/index.ts`; wire into `bin/host.ts` as described above.
- Supervisor unit tests (vitest, `packages/cadre-host`) over a fake orchestrator
  plus a real `DonationStore` in a tmp dir with an injected clock. The existing
  `FakeOrchestrator` in `donation/__tests__/donation-service.test.ts:38` is the
  pattern to copy, but its `isRunning()` returns a hardcoded `true` — the
  supervisor tests need per-`dockerId` control, so extend or fork it. Cases:
  - a `seeded`-but-stopped node is respawned, its `dockerId` changes, and
    `status` stays `seeded`;
  - a `terminated` record is never respawned;
  - a second reconcile inside the backoff window does not re-attempt, and does
    once the clock advances past it;
  - a `createContainer` that always throws lands the record on `status: 'error'`
    after `DONATION_RESPAWN_MAX_ATTEMPTS` and then stops attempting;
  - a record with no `bootstrapNodes`/`ownerKeys` (pre-existing on disk) is
    skipped without throwing and without ending the sweep early.
- Document the respawn contract in `docs/cadre-host.md`, under the existing
  "Node donation (the primary role)" section (`:62`) near "The donate-a-node
  lifecycle" (`:73`): what is supervised, the backoff/give-up numbers, and that a
  terminated loan stays terminated.
- `yarn workspace @serfab/cadre-host build && yarn workspace @serfab/cadre-host test`,
  plus `yarn lint`.

## Honest gaps

- No executed reproducing test exists yet; the failure was established by code
  trace. Every claim above was re-verified against current `master`, but line
  numbers will drift.
- The supervisor is only unit-tested against a fake orchestrator. Nothing in this
  ticket proves a *real* respawned child rejoins the requester's cadre — that
  would need the cross-package `cadre-host-node-donation` integration scenario,
  which is out of scope here and worth flagging to whoever reviews.
