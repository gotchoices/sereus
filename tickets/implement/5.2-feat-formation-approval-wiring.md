description: Actually consult the outside approval service when someone tries to join a network that requires sign-off, and pass its answer to the database that demands it — today such invitations simply cannot be used.
prereq: feat-formation-approval-client, debt-formation-provision-step-timeout
files: packages/cadre-core/src/strand-formation-manager.ts, packages/cadre-core/src/control-formation-recorder.ts, packages/cadre-core/src/strand-solicitation.ts, packages/cadre-core/test/strand-formation-consent.spec.ts, packages/cadre-core/test/strand-solicitation.spec.ts, packages/cadre-core/test/control-formation-invite.spec.ts, packages/integration-tests/src/scenarios/strand-formation-e2e.integration.ts, packages/integration-tests/src/scenarios/multi-party-workflows.integration.ts
difficulty: medium
----

# Wire the approval hook into the redemption path

## Progress (third run — Phases 1–4 ALL LANDED; only Phase 5 gates + handoff remain)

Three agent runs so far. Runs 1–2 landed Phases 1–2 (interfaces + recorder + DB nonce mint);
this third run landed Phases 3 and 4 as real code edits. **All production and test code is
written. Nothing has been reverted; do not revert anything.** What remains is purely:
run the gates, fix any fallout, write the review/ handoff, delete this ticket.

### Landed in runs 1–2 (verified still present)

- `strand-solicitation.ts` — `ResolvedHostStrand` carries `validationUrl: string | null` on
  `unbound` + `bound`; `recordUsage` / `provisionAndRecord?` take object shapes with
  `peerId` + `disclosure` (positional forms GONE, no back-compat);
  `recordFormationComplete(token, peerId, strandId, disclosure = '')` forwards the object.
- `control-database.ts` — `mintUsageStampId(): string` above `redeemInvitation`.
- `formation-approval.ts` — `'unenrolled'` in `FormationApprovalFailure` (local pre-check only).
- `control-formation-recorder.ts` — fully wired: ctor `(db, options?: { approver? })`
  defaulting `createHttpFormationApprover()`; private `obtainApproval` mints nonce, calls
  approver, local verify → `'malformed'`, enrollment check → `'unenrolled'`; both write paths
  read the invite for `validationUrl` and thread `disclosure` + `usageStampId` +
  `validationKey` + `validationSignature` into the DB calls; `resolveStrand` returns
  `validationUrl`.

### Landed THIS run (Phase 3 — manager)

`packages/cadre-core/src/strand-formation-manager.ts`:

- New imports: `fromString as uint8ArrayFromString` from `uint8arrays`;
  `FormationApprovalError` + `type FormationApprovalFailure` from `./formation-approval.js`.
- New module constants under the `log` line: `MAX_DISCLOSURE_BYTES = 8 * 1024` (doc comment
  explains the bound) and `APPROVAL_REJECTION_REASONS: Record<FormationApprovalFailure, string>`
  with exactly the five reasons from the spec table.
- `provisionAsResponder` now takes `disclosure` (not `_disclosure`): serializes ONCE to
  `disclosureText = JSON.stringify(disclosure)` and enforces the byte cap BEFORE
  `resolveStrand` (reason `'Disclosure too large'`); fallback literal is
  `{ kind: 'unbound', validationUrl: null }`; bound path calls
  `recorder!.recordUsage({ token, peerId: initiatorPartyId, strandId, disclosure: disclosureText })`;
  unbound path threads `disclosureText` to `provisionUnbound(token, initiatorPartyId, disclosureText)`.
- Catch block: `err instanceof FormationApprovalError` → log-then-map via
  `APPROVAL_REJECTION_REASONS[err.failure]`; else the spent-approval `NOTE:` comment (nonce
  never inserted, `unique` UsageStampId bars re-presenting, retry needs fresh approval /
  second human review; no usage row written so invite not consumed) then
  `'Formation conflict, retry'` unchanged.
- `provisionUnbound(token, initiatorPartyId, disclosureText)` passes the object shape to
  `provisionAndRecord`; `strandProvisioner` + placeholder branches untouched.
- `strand-formation-protocol.ts` :39-55 comment present-tensed: "once
  feat-formation-approval-wiring lands" → "when the invite carries a `ValidationUrl`", and
  "(10 s, feat-formation-approval-client)" → "(10 s, `formation-approval.ts`)".

### Landed THIS run (Phase 4 — tests)

- `strand-solicitation.spec.ts` :167-197 — mock captures the params object; expectation is
  `{ token: 'token-1', peerId: 'key-1', strandId: 'strand-1', disclosure: '' }`. The inert
  no-arg `recordUsage: async () => {}` mocks elsewhere in the file were left alone (a no-arg
  fn is assignable to the object signature).
- `control-formation-invite.spec.ts` — object-shaped `recordUsage` (:~1200/:~1208, with
  `disclosure: ''`) and `provisionAndRecord` (:~1269); `validationUrl: null` added to the
  `bound` expectation and BOTH `unbound` expectations; `missing` untouched.
- Integration mocks — `strand-formation-e2e.integration.ts` + `multi-party-workflows.integration.ts`:
  `recordUsage: async ({ token, peerId, strandId })` + map value field renamed
  `initiatorKey` → `peerId` (grep confirmed no other reader of that field). Their positional
  `recordFormationComplete` call sites compile unchanged (`disclosure` defaults).
- `strand-formation-consent.spec.ts` — full new coverage:
  - `responder(approver?: FormationApprover)` → `new ControlFormationUsageRecorder(db, approver ? { approver } : undefined)`.
  - `beforeAll` enrolls `validationPublicKey` via `db.insertValidationKey(validationPublicKey, ownerPublicKey, signMessage)`.
  - `recordingApprover(answer)` helper records every `FormationApprovalRequest` in `.seen`.
  - `contactFor(token, disclosure = REAL_DISCLOSURE)` gained the optional disclosure param.
  - New cases (h)–(m), all per original spec: (h) bound `ValidationUrl` — one row,
    `Disclosure === JSON.stringify(REAL_DISCLOSURE)`, `UsageStampId` read back equals the
    nonce the approver saw, host strand + membership key in the frame; (i) unbound through
    `provisionAndRecord`, approver signed over the minted strand id; (j) refused /
    unavailable / tampered-fields signature / non-enrolled stray keypair → the four distinct
    reasons, zero rows each, then the SAME single-use invite redeems with a good approver;
    (k) no-`ValidationUrl` invite — `approver.seen.length === 0`, real disclosure text
    recorded; (l) `purpose: 'x'.repeat(9000)` → `'Disclosure too large'`, approver never
    invoked, no row; (m) 6 s slow approver succeeds, test timeout 40 s (real wall-clock).
  - Header doc comment extended with (h)–(m). Existing (a)–(g) untouched.

### Remaining work (Phase 5 ONLY — verify, fix fallout, hand off)

**No gate has been run yet this ticket.** The in-editor diagnostics during the run showed
errors only from the STALE cadre-core `dist` `.d.ts` (integration-tests resolves the built
types, which still carry the old positional signatures) — expected to clear on rebuild.

- `yarn build` (workspace). Watchpoints if anything fails:
  - integration-tests destructure errors = stale cadre-core dist → cadre-core must build first
    (workspace build order should handle it).
  - Any OTHER package implementing `FormationUsageRecorder` (cadre-host / cadre-cli /
    reference apps) was NOT swept — the repo-wide grep of call sites came back clean, but
    implementors were not individually checked; the type-check is the backstop. Fix any by
    switching to the object shapes (no back-compat).
  - vitest two-arg `expect(actual, message)` is used in case (j) — supported by vitest; if
    the repo's expect types disagree, fold the label into the asserted value instead.
- `yarn lint`.
- cadre-core vitest suite (stream output via tee; case (m) really waits 6 s). Integration
  suites are NOT agent-runnable — compile/type-check only, and say so in the handoff.
- Write the review/ handoff into tickets/review/ and delete this ticket. Must note:
  - every redemption now records real disclosure text where it recorded `''` before;
    existing rows keep `''`; nothing reads the column;
  - the default-ON HTTP approver in `ControlFormationUsageRecorder` (constructor comment
    explains why);
  - the spent-approval semantics `NOTE:` in the manager's catch-all (tripwire, indexed in
    `## Review findings`, not a ticket);
  - the `Omit<..., 'usageStampId' | 'validationUrl'> & { validationUrl: string | null }`
    type deviation in `obtainApproval` (spec's narrower Omit collapses `string & (string|null)`
    to `string`);
  - integration suites compile-only.

## Edge cases & interactions (unchanged original spec — all now covered by (h)–(m) or code)

- Approval refused / hook unreachable → mapped reason, no row, invite not consumed. Covered (j).
- Non-enrolled key → `'Formation approval key is not enrolled'`, never the generic retry. Covered (j).
- Signature over different fields → local verify catches, `'Formation approval invalid'`. Covered (j).
- Nonce sign/insert mismatch → asserted directly. Covered (h)/(i).
- No `ValidationUrl` → approver not called. Covered (k).
- Concurrent single-use race → loser's approval spent; the manager `NOTE:` covers it; no new ticket.
- Bound + `missing` host strand → rejected before hook contact (existing (g), unchanged).
- Oversized disclosure → rejected before hook AND before any DB read. Covered (l).
- Hook latency vs budgets → 10 s < 12 s ordering; covered (m).
- Disclosure is attacker-influenced replicated text → 8 KiB cap is the bound; nothing parses it.
- Do NOT add a second pre-write `isTokenUsed` check — it cannot close the race; the
  PK-collision path must stay a clean rejection.

## End
Work ticket as described above.
Do NOT commit — runner handles commits after you complete.
