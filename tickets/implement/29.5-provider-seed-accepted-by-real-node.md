---
description: Finish stabilizing the new end-to-end test that proves a node started by the hosting provider accepts the join bundle the provider delivers and refuses a stranger's. The test is written and its core assertion has passed once, but it cannot go green until a separately-ticketed startup crash in multi-node cadres is fixed.
prereq: verify-ddl-retry-engages
files: packages/integration-tests/src/scenarios/provider-seed-accepted.integration.ts, packages/integration-tests/src/harness/provider-process-orchestrator.ts, packages/integration-tests/src/harness/index.ts
difficulty: medium
---

# Stabilize the provider-seed-accepted scenario (runs 1–3; run 3 interrupted on soft token budget)

## Where this stands

**All code exists.** Run 2 landed the harness
(`ProviderProcessOrchestrator` — starts a real `@serfab/cadre-cli` child from
the provider's own `buildNodeEnv()` output; see its module doc) plus deps.
Run 3 wrote the full scenario
(`provider-seed-accepted.integration.ts`, steps 1–5), built all four cadre
workspaces, and got a clean
`yarn workspace @serfab/integration-tests typecheck`. `yarn lint` has NOT been
run yet.

Two live executions of the scenario:

- **Steps 1–2 pass reliably** (owner authority up; `ContainerService`
  provisions node A pinned to the owner, record reaches `running` via the
  service's real enrollment loop, seedToken/seedEndpoint persisted).
- **Step 3 — the assertion the original ticket existed for — PASSED in run 1**:
  node A accepted the owner-signed seed delivered through the real
  `ContainerService.applySeed` path with `peersAdded >= 1`. In run 2 it failed
  in 35ms because the owner's `addDrone` threw a transient internal error
  (`nodeCode: 'internal'`) — a test-robustness gap, fix below.
- **Step 4 fails 2/2 with a real product defect**: node B — the third real
  node in the party — crashes during startup schema DDL. That is
  the schema-init retry now under `verify-ddl-retry-engages` (the prereq): the
  retry has landed but has not yet been confirmed to engage against a real
  failure. Do not work around it here.
- **Step 5** (restart node A without a pin; second seed still accepted via the
  durable trusted-owner anchor) has never been reached cleanly — run 1 hit an
  owner-side query error after A's restart (already mitigated: the second
  seed's mint + apply now sit inside a retry poll), run 2 cascaded from
  step 3's failure.

The scenario currently FAILS in-tree (steps 3–5, intermittent/blocked as
above). It only runs under the integration suite, so other tickets' targeted
test runs won't hit it, but a full-suite run will — that is expected until the
prereq lands.

## Remaining work (after the prereq fix lands)

- Wrap step 3's `addDrone` + first `applySeed` in a `waitUntil` retry poll,
  exactly like step 5 already does (owner-side admin calls can transiently
  error while cluster membership is in flux). Keep the existing "times out if
  the pin never reached the node" semantics.
- Re-run the scenario end to end until green twice consecutively:
  `yarn workspace @serfab/integration-tests test src/scenarios/provider-seed-accepted.integration.ts 2>&1 | tee /tmp/provider-seed.log`
  (stream it — silent redirection can trip the runner's 10-minute idle
  timeout). Rebuild cadre-core/cli/host/provider first if their dists are
  stale; the suite's global-setup guard enforces the same.
- If step 4's single `applySeed` against node B proves racy even after the
  prereq fix (a refusal reason other than the trust policy's), poll until the
  refusal error matches `/(neither|not).*(anchored|pinned)/i` — but a SUCCESS
  on node B must fail the test immediately.
- `yarn lint` (never run this ticket).
- Handoff into review/ noting: measured wall-clock; the no-pin variant was
  deliberately dropped (step 4's stranger-pin already proves discrimination; a
  third child process wasn't worth it); `entrypoint.sh` itself still untested
  (the orchestrator reproduces its derivations in-process); the
  `waitForEnrollment` 60s hard-coded timeout tripwire (silently leaves records
  `enrolling`; also called out in the fix ticket); the small helpers
  (`writeIdentity`, `withPeerId`, `readNodeLocalStore`) duplicated from the
  donation scenario rather than lifted into the harness — flag as a possible
  DRY cleanup for review.

## Design decisions already baked into the scenario (don't re-derive)

- Owner authority via cadre-host's `HostProcessOrchestrator`, port band
  20200–20339 (19600–20199 are claimed by the cadre-host suites),
  `libp2pPort: 0`.
- No assertion on `createContainer`'s returned `status`: `MemoryStore` hands
  back live references, so the async provisioning loop mutates it under the
  test ('pending' → 'creating' race, seen in run 1).
- Step 5 restarts node A through the orchestrator directly (same containerId →
  same volume → same identity, NO pins) because `ContainerService` can only
  mint fresh `ctr_` ids; the store record is then patched with the fresh
  endpoints/token and `status: 'running'`.
- Negative assertions are gated on the node being `running` first; failure
  messages dump the child's `node.log`.
