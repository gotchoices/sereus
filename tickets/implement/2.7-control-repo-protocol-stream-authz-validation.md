----
description: Finish validating the lockdown of the shared control database against un-admitted peers — all code, tests, and docs are done and green; the only remaining work is running the rest of the integration scenario suite to confirm nothing regressed, then writing the review handoff.
files:
  - packages/integration-tests/src/scenarios/control-stream-authz.integration.ts (NEW run 5, GREEN first try)
  - packages/integration-tests/src/scenarios/control-db-two-node-convergence.integration.ts (REPAIRED run 5, GREEN)
  - packages/cadre-core/src/membership-connection-gater.ts (module doc REWRITTEN run 5 — two-layer description)
  - packages/cadre-core/src/cadre-node.ts (landed earlier; NOTE tripwire at reconcile refresh ~1408)
  - packages/cadre-core/test/control-stream-authorization.spec.ts (GREEN)
  - packages/cadre-core/test/cadre-node-control-cohort.spec.ts (GREEN)
  - docs/architecture.md (per-stream gate bullet ADDED run 5)
  - docs/STATUS.md (step-6 "blocked upstream" tail REPLACED with landed description, run 5)
difficulty: easy
----

# Per-stream control-DB authorization: remaining scenario sweep + review handoff

Continuation of `control-repo-protocol-stream-authz-tests-docs` (chain root:
`control-repo-protocol-stream-authz`). Run 5 (2026-07-29) finished ALL
implementation, tests, docs, and primary validation, then hit budget
mid-sweep. **Nothing is known broken.** Remaining: run the rest of the
integration scenarios (regression sanity under the now-armed stream gate),
then write the review/ handoff.

## Run-5 state — everything below is DONE and VERIFIED GREEN

- NEW scenario `control-stream-authz.integration.ts` — **GREEN first try
  (1.3s test time)**. Three nodes, one party: owner A (storage, relay),
  member M (vouched via `A.authorizePeer` BEFORE dialing), outsider O
  (different party, dials in during an enrollment window opened by
  `A.createInvite`). Drives the repo protocol RAW via
  `RepoClient.create(repoPeerIdFromString(aPeerId), stub, '/optimystic/control-<partyId>')`
  where the `IPeerNetwork` stub is `dialProtocol` over the dialer's control
  node (re-parsing the structural peer id via `@libp2p/peer-id`). Asserts:
  M's pend+commit of block `stream-authz-B1` succeeds; O's identically-shaped
  pend of `stream-authz-B2` `rejects.toThrow()`; M's `get([B1,B2])` shows B1
  present and B2 absent (denied write never reached the repo); O's
  CONNECTION to A still `status === 'open'` on BOTH sides after the denied
  stream. No redirect flake materialized (cluster {A,M} always contains A →
  A serves locally; header comment documents why).
- REPAIRED `control-db-two-node-convergence.integration.ts` — **GREEN**:
  `bootPair` now runs `await A.authorizePeer(B.peerId!.toString())` after
  `B.start()` so B's pull-on-read streams pass A's per-stream gate; B still
  pins nobody (row-presence-vs-trust point intact). Module header updated to
  explain the vouch.
- Docs all rewritten: `membership-connection-gater.ts` module doc (both the
  intro enumeration and the stale "no per-stream authz hook yet /
  blocked-ticket" paragraph — now describes fail-open connection gate over a
  live read vs fail-closed stream gate over the materialized snapshot);
  `docs/architecture.md` gained a **Per-stream control-DB gate** bullet
  (after the connection-gate bullet, seed-delivery section); `docs/STATUS.md`
  step-6 tail replaced with a "per-stream half has LANDED (2026-07-29)"
  paragraph. Stale refs to `blocked/control-repo-protocol-stream-authz-optimystic`
  now exist only in `tickets/complete/` archives (historical — left alone).
- Validation already run, all green: `yarn build` (root), `yarn lint`
  (root, clean), `yarn workspace @serfab/integration-tests typecheck`
  (clean), full `yarn workspace @serfab/cadre-core test` — **825 passed,
  1 skipped, 0 failed**; both scenarios above individually.

## TODO — remaining sanity sweep, then handoff

Run each scenario individually from `packages/integration-tests`
(`yarn vitest run src/scenarios/<file> 2>&1 | tee <log>` — stream output;
one at a time):

- `membership-connection-gater.integration.ts`
- `control-cohort-auto-convergence.integration.ts`
- `convergence-stress`, `multi-party-workflows`, `push-wake-e2e`,
  `rbac-signed-write`, `strand-formation-e2e`,
  `strand-membership-closed-strand-e2e`, `websocket-chat`,
  `cadre-host-bootstrap`, `cadre-host-trust-circle`

KNOWN pre-existing failures listed in `tickets/.pre-existing-known.md`
(`bug-control-cohort-no-auto-dial`, `bug-strand-three-party-replication`) —
note awareness in the handoff, do not chase. Any NEW failure is likely the
now-armed stream gate denying a scenario's un-vouched pull: the fix pattern
is the convergence repair (owner vouches the reader before its pulls) —
apply the same one-liner + comment, don't weaken the gate.

Then write the review/ handoff ticket (delete this one). Content to carry
into its "for the reviewer" notes — the accumulated index for the eventual
`## Review findings`:

- NOTE tripwire in `cadre-node.ts` (~1408): reconcile pass does two
  CadrePeer reads (refresh + sibling enumeration); share one row-set if
  reads ever get costly.
- `cadre-node-control-cohort.spec.ts` single-flight test now asserts 2
  queries per pass (2-not-4 proves coalescing) and stubs
  `queryRevokedStamps`.
- Restart staleness (documented in both docs): a member added while a node
  was down is admitted only after that node's next reconcile refresh —
  bounded by design; no scenario depends on faster pickup.
- `runReconcileControlCohort` re-guards `_running` after the refresh await
  (verified run 4).
- Enrollment ordering verified non-deadlocking (run 1): invite flow rides
  ungated `/sereus/seed/1.0.0`; a joining node's gate is inert while its
  anchor/snapshot are empty.
- `ops/docker/libp2p-infra` roles never speak the four DB protocols —
  unaffected.
- Reviewer should treat the unit matrix
  (`control-stream-authorization.spec.ts`, 11 tests) + the new scenario as
  the floor: e.g. cluster/sync/block-transfer protocols are gated by the
  same predicate but only repo is driven raw end-to-end.
