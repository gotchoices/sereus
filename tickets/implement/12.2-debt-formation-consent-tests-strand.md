description: When someone joins one of a party's networks, the joiner now signs its own join record and the responder verifies it before accepting. Production code is finished and the database-level tests are handled by a sibling ticket; this ticket updates the network-protocol test suites and two integration-test call sites, then runs the full build/lint/test gate.
prereq: debt-formation-consent-tests-db
files: packages/cadre-core/test/formation-consent-helper.ts, packages/cadre-core/test/strand-formation-consent.spec.ts, packages/cadre-core/test/strand-solicitation.spec.ts, packages/cadre-core/test/strand-formation-protocol.spec.ts, packages/cadre-core/test/formation-approval.spec.ts, packages/cadre-core/test/peer-authorization.spec.ts, packages/cadre-core/test/validation-key-enrollment.spec.ts, packages/integration-tests/src/scenarios/strand-formation-e2e.integration.ts, packages/integration-tests/src/scenarios/multi-party-workflows.integration.ts, packages/cadre-core/src/strand-formation-protocol.ts (read-only), packages/cadre-core/src/strand-formation-manager.ts (read-only), packages/cadre-core/src/strand-solicitation.ts (read-only)
difficulty: hard
----

# Joiner-signed formation records — strand-layer tests + full gate (part 2 of 2)

Continuation after another soft-budget stop. **NO test edits have landed yet — the
tree is clean**; everything below is verified reconnaissance from a full read of the
sources and every failing spec, so the next run can go straight to editing. ALL
source wiring is committed (tree was clean at start on `master`, after
commit `e94e943`). The prereq's `test/formation-consent-helper.ts` EXISTS with
`mintJoiner()` / `signJoinerConsent()` / `mintConsent()` (control-DB flavor:
plain ed25519 keys, no libp2p peer id).

## Verified facts (do not re-derive)

- `tsc -p packages/cadre-core/tsconfig.typecheck.json --noEmit` (run via
  `yarn --cwd packages/cadre-core exec tsc -p tsconfig.typecheck.json --noEmit`)
  currently reports errors in EXACTLY these six test files; re-run it to drive the work:
  - `formation-approval.spec.ts` 32, 125 — `peerId` → `peerKey` in `FormationApprovalRequest`.
  - `peer-authorization.spec.ts` 197 — `peerId: row.peerKey` → `peerKey: row.peerKey` in a `formationVouchMessage` literal.
  - `strand-formation-consent.spec.ts` 120 (contact literal missing `peerKey`/`usageStampId`/`peerSignature`), 509 (`peerId` in vouch-tamper scenario → `peerKey`).
  - `strand-formation-protocol.spec.ts` 68 (contact literal), 238/400/425 (`provisionStrand` mocks written as `(_t, _p, _d, signal)` → `(contact, signal?)`; also trips noImplicitAny).
  - `strand-solicitation.spec.ts` 173 (mock `recordUsage` capture typed with old `peerId` param shape), 183/196 (`recordFormationComplete` now takes 4-5 args).
  - `validation-key-enrollment.spec.ts` 172, 176 (`peerId` in vouch literal + `recordFormationUsage` now needs the consent triple).
- `FormationListener.runSession` (`src/strand-formation-protocol.ts:521`) runs
  `isJoinerConsentValid` (line 492) BEFORE `validateToken`. It requires, in order:
  1. `contact.peerKey` passes `requireEd25519PublicKeyB64`;
  2. `ed25519PublicKeyB64FromPeerId(contact.partyId)` (src/seed-bootstrap.ts:87 — parses a
     libp2p peer-id string, returns its embedded ed25519 key as base64url) is non-null and
     `=== contact.peerKey` — **so `contact.partyId` MUST be a real libp2p ed25519 peer-id
     string embedding the key; a fake string like `'initiator-key'` is rejected**;
  3. `verifyFormationConsent` over digest fields `{ token, usageStampId, peerKey,
     disclosure: canonicalJson(contact.disclosure) }` against `contact.peerSignature`.
  Rejection = ordinary frame `{ approved: false, reason: 'Invalid joiner consent' }`.
- **Consequence: EVERY hand-built contact in the protocol + consent specs is now rejected
  with 'Invalid joiner consent' before reaching the path its test targets** — the shared
  module-level `contact` const in `strand-formation-protocol.spec.ts:68` and the
  `contactFor()` builder in `strand-formation-consent.spec.ts:119` must both mint real
  joiner identities. Tests REUSING one contact object across sessions (retry/adoption tests
  in the protocol spec) stay valid — the consent check is pure crypto, no nonce burned at
  the protocol layer.
- `provisionAsResponder` (`src/strand-formation-manager.ts:289`) serializes the disclosure
  ONCE with `canonicalJson(contact.disclosure)` and that exact string is signed/stored.
  So consent for a hand-built contact signs over `canonicalJson(disclosure)`, and the
  consent spec's two assertions `usage?.Disclosure === JSON.stringify(REAL_DISCLOSURE)`
  (lines ~437-438, ~573) should become `canonicalJson(REAL_DISCLOSURE)` (import from
  `../src/canonical-json.js`).
- Schema (`schemas/control.qsql:482`): `FormationUsage.UsageStampId text not null unique`
  — nonce reuse fails the insert, lands in the manager's generic catch (line 335-355)
  → `'Formation conflict, retry'`, invite unspent. Columns are now `PeerKey`/`PeerSig`
  (no `PeerId` column) — `validation-key-enrollment.spec.ts:192` selects `PeerId`, fix to
  `PeerKey` and match `{ Token, PeerKey, StrandId }`.
- `formationConsentMessage` (src/control-database.ts:208) takes
  `{ token, usageStampId, peerKey, disclosure }`.
- Solicitation initiator path (src/strand-solicitation.ts:303-348): mints libp2p keypair,
  `memberKey = peerIdFromPrivateKey(pk).toString()`, `ed25519KeyPairFromLibp2p(pk)`
  → `{ privateKeyB64, publicKeyB64 }`, `usageStampId = generateStampId(memberKey)`,
  `fullDisclosure = { ...disclosure, partyId: memberKey }` (overrides caller partyId),
  signs `formationConsentMessage({ token, usageStampId, peerKey: publicKeyB64,
  disclosure: canonicalJson(fullDisclosure) })`. So end-to-end tests that go through
  `service.formStrand(invitation, disclosure, node)` need NO test-side consent — real
  consent is minted automatically (the solicitation spec's transport tests and both
  integration files' `formStrand` calls are fine as-is).
- `recordFormationComplete(token, peerKey, strandId, consent: { usageStampId;
  peerSignature }, disclosure = '')` — widened 4th param; forwards to `recordUsage` which
  now takes `{ token, peerKey, peerSignature, usageStampId, strandId, disclosure, signal? }`
  (see `ControlFormationUsageRecorder.recordUsage` at src/control-formation-recorder.ts:193
  and `provisionAndRecord` at :271 for exact shapes).
- The listener consent pre-check runs for ALL responder configs including
  provisioner-only/no-recorder fallbacks (`provisionUnbound` precedence:
  provisionAndRecord > strandProvisioner > placeholder) — those tests' contacts also need
  real consent, though nothing verifies it downstream of the listener.

## Work plan (concrete, in order)

### 1. Extend `test/formation-consent-helper.ts` with a strand-layer joiner

```ts
import { generateKeyPair } from '@libp2p/crypto/keys';
import { peerIdFromPrivateKey } from '@libp2p/peer-id';
import { ed25519KeyPairFromLibp2p } from '../src/ed25519-key.js';
import { canonicalJson } from '../src/canonical-json.js';

/** A joiner with a REAL libp2p identity: partyId embeds peerKey (the listener pins them). */
export interface TestContactJoiner extends TestJoiner { partyId: string; }

export async function mintContactJoiner(): Promise<TestContactJoiner> {
  const libp2pKey = await generateKeyPair('Ed25519');
  const { privateKeyB64, publicKeyB64 } = ed25519KeyPairFromLibp2p(libp2pKey);
  return { privateKey: privateKeyB64, peerKey: publicKeyB64, partyId: peerIdFromPrivateKey(libp2pKey).toString() };
}

/** Consent triple for a strand-layer contact: signs over canonicalJson(disclosure). */
export function mintContactConsent(joiner: TestContactJoiner, token: string, disclosure: unknown): JoinerConsent {
  const usageStampId = generateStampId(joiner.partyId);
  return {
    peerKey: joiner.peerKey, usageStampId,
    peerSignature: signJoinerConsent(joiner, { token, usageStampId, disclosure: canonicalJson(disclosure) }),
  };
}
```
(`signJoinerConsent` already signs `formationConsentMessage` with the joiner's
`privateKey` — works unchanged because `TestContactJoiner.privateKey` holds the b64url seed.)

### 2. `strand-formation-consent.spec.ts`

- Make `contactFor` async: mint a `TestContactJoiner`, return
  `{ token, partyId: joiner.partyId, ...mintContactConsent(joiner, token, disclosure), disclosure, cadrePeerAddrs }`.
  Update every `encodeFrame(contactFor(...))` call site to `encodeFrame(await contactFor(...))`
  (~14 sites, all inside `it` bodies).
- Line 509 tamper scenario: `peerId: 'tampered-' + req.peerId` → `peerKey: 'tampered-' + req.peerKey`.
- Disclosure assertions ~437-438 and ~573: `JSON.stringify(REAL_DISCLOSURE)` → `canonicalJson(REAL_DISCLOSURE)`.
- ADD new tests (settled design, do not re-open):
  - **Invalid joiner consent**: valid bound invite; send (i) contact with tampered
    `peerSignature`, (ii) contact whose `partyId` is a DIFFERENT joiner's peer id (key
    mismatch), (iii) contact with garbage `peerKey`. Each → `approved: false`, reason
    `'Invalid joiner consent'`, no `partyId`/`provisionResult` in reply, usage count 0,
    then the SAME invite still redeems cleanly with a well-formed contact (not burned).
  - **Nonce reuse**: bound invite `totalUses: 2`; build ONE contact, send twice. First
    approved; second → `approved: false`, reason `'Formation conflict, retry'`, usage
    count stays 1, and a FRESH contact (new joiner/nonce) for the same token then succeeds
    (count 2).

### 3. `strand-formation-protocol.spec.ts`

- Replace the module-level `contact` const with one built via top-level `await`
  (vitest ESM handles it): mint joiner, build contact for token `'invite-real'`, keep
  `realDisclosure` as-is (its `partyId: 'initiator-key'` INSIDE the disclosure is fine —
  the listener never cross-checks disclosure.partyId).
- Three mocks at 238/400/425: `(_t, _p, _d, signal)` / `(_token, _partyId, _disclosure, signal)`
  → `(_contact: FormationContactMessage, signal?: AbortSignal)`.
- ADD listener-level consent tests (cheap, no DB): tampered signature / partyId-key
  mismatch / malformed peerKey each → `'Invalid joiner consent'`, `identityDisclosed()`
  false, AND `validateToken` never called (consent runs first — assert with a counting mock).

### 4. `formation-approval.spec.ts`

`baseRequest` field `peerId: '12D3KooWJoiner'` → `peerKey: '12D3KooWJoiner'` (any string
fine — this spec never hits the listener), and the MUTATIONS entry
`['peerId', { peerId: '12D3KooWOther' }]` → `['peerKey', { peerKey: '12D3KooWOther' }]`.

### 5. `peer-authorization.spec.ts`

Line 197: `peerId: row.peerKey` → `peerKey: row.peerKey` in the vouch-as-consent test.

### 6. `validation-key-enrollment.spec.ts` (~160-196, REAL DB — consent must be genuine)

- Use prereq helper: `const joiner = mintJoiner();` and
  `const consent = mintConsent(token, '', joiner);` — its `usageStampId` replaces the
  hand-rolled `'usage-retro-' + rand()`.
- Vouch: `formationVouchMessage({ token, usageStampId: consent.usageStampId, strandId,
  peerKey: joiner.peerKey, disclosure: '' })`.
- `db.recordFormationUsage({ token, strandId, ...consent, disclosure: '', validationKey,
  validationSignature })` — check the exact param name set against
  `recordFormationUsage`'s signature in src/control-database.ts (typecheck error at 176
  lists it: `{ token, strandId, peerKey, disclosure?, usageStampId, peerSignature, nowMs?,
  validationKey?, validationSignature?, signal? }`).
- Line 192 select: `PeerId` → `PeerKey`; matcher `{ Token, PeerKey: joiner.peerKey, StrandId }`.

### 7. `strand-solicitation.spec.ts`

- ~169-191: retype the `recorded` capture to the new `recordUsage` param object
  (`{ token, peerKey, peerSignature, usageStampId, strandId, disclosure, signal? }`);
  call `recordFormationComplete('token-1', 'key-1', 'strand-1', { usageStampId: 'stamp-1',
  peerSignature: 'sig-1' })` and assert the recorder received the triple (mock doesn't
  verify — plausible fixtures fine). Line 196 no-recorder call: add the same 4th arg.
- The three inert/mock recorders at ~85/~172/~227 with `recordUsage: async () => {}` —
  structurally compatible; only 173's typed capture errors. Leave the rest unless tsc flags.
- Transport-integration tests go through real `formStrand` — no changes expected.

### 8. Integration tests (break repo `yarn build` until fixed)

- `packages/integration-tests/src/scenarios/strand-formation-e2e.integration.ts:225` and
  `multi-party-workflows.integration.ts:210, 304`: add 4th `consent` arg to
  `recordFormationComplete(token, key, strandId, { usageStampId, peerSignature })`.
  Recorders there are mocks that don't verify → plausible fixtures fine; if any recorder
  is real, mint via the helper.
- Hand-written `partyId: 'party-b-excl-…'` disclosure INPUTS are safe — solicitation
  overrides `partyId` with the minted memberKey.

### 9. Gate (full, streamed — never silent-redirect; 10-min idle kill)

- `yarn build 2>&1 | tee /tmp/build.log`
- `yarn lint 2>&1 | tee /tmp/lint.log`
- cadre-core unit suite (from packages/cadre-core): `yarn test 2>&1 | tee /tmp/test.log`
- formation integration scenarios (check package.json in packages/integration-tests for
  the runnable subset; skip anything whose wall-clock routinely exceeds ~10 min and
  document the deferral).

### 10. Handoff

Write the review/ ticket covering BOTH parts (`debt-formation-consent-tests-db` — already
in review via commit `e94e943` — and this one): what the joiner-consent sweep now covers,
the new negative tests, known gaps. The exhaustive negative matrix + docs
(`docs/api.md` hook-body rename) remain `debt-formation-consent-tests-docs`. Delete this
ticket file.

## Edge cases to hold (settled design — do not re-open)

- A consent-check rejection is an ordinary `approved: false` ('Invalid joiner consent'),
  never an abort; `FormationAbortedError` / settle-grace paths unchanged.
- Joiner nonce reuse surfaces as 'Formation conflict, retry' with the invite unspent.
- Listener consent pre-check runs for ALL responder configs — provisioner-only /
  no-recorder tests must send well-formed contacts even though those branches write no
  `FormationUsage` row.
- ValidationUrl invite needs BOTH signatures over the same nonce + same canonical
  disclosure bytes.
