description: When someone joins one of a party's networks, the record of that join names who joined, but nobody checks that the named person actually agreed to it. Make the joiner sign its own join, carry that signature over the wire, and refuse the record without it. The schema and database write paths are done; this ticket finishes the protocol/initiator wiring and the tests.
prereq: debt-formation-consent-helpers
files: packages/cadre-core/src/strand-formation-protocol.ts, packages/cadre-core/src/strand-formation-manager.ts, packages/cadre-core/src/strand-solicitation.ts, packages/cadre-core/test/control-formation-invite.spec.ts, packages/cadre-core/test/control-formation-recorder.spec.ts, packages/cadre-core/test/control-revocation-replay.spec.ts, packages/cadre-core/test/strand-formation-consent.spec.ts, packages/cadre-core/test/strand-solicitation.spec.ts
difficulty: hard
----

# The joining peer signs its own formation record — finish wiring + tests

FOURTH run stopped by soft token budget. Unlike prior runs, THIS run landed real code:
**Phases 1 and 2 are COMPLETE and diagnostics-clean in the edited files; Phase 3 is
partially done.** The working tree currently has known compile errors listed below —
they are the exact remaining seam. Do NOT revert anything; finish forward.

## Already DONE this run (do not redo, do not re-read whole files)

**Phase 1 — schema (complete).** `schemas/control.qsql` + the embedded mirror in
`packages/cadre-core/src/control-schema.ts`:
- `FormationUsage.PeerId` → `PeerKey` (comment rewritten), new `PeerSig text not null`
  column after it.
- `'vouch'` digest in `Authorized` now uses `new.PeerKey`; its doc comment updated.
- New `PeerConsented` check on insert verifying
  `digest('CadreControl.FormationUsage', 'consent', new.Token, new.UsageStampId,
  new.PeerKey, new.Disclosure)` against `new.PeerSig`/`new.PeerKey`, with the settled
  NOTE explaining StrandId's deliberate absence.
- Context list is now `(Now datetime, ValidationKey text null, ValidationSignature
  text null)` — `PeerSignature` removed; trailing comment rewritten.

**Phase 2 — write paths (complete).**
- `control-database.ts`: `formationVouchMessage` field `peerId`→`peerKey` (doc
  updated: joiner mints nonce); `mintUsageStampId` DELETED; `redeemInvitation` and
  `recordFormationUsage` take required `peerKey`, `usageStampId`, `peerSignature`
  (no mint fallback, docs updated); `execFormationUsageInsert` inserts
  `(Token, UseNumber, UsageStampId, PeerKey, PeerSig, Disclosure, StrandId,
  StrandStampId)` with context `Now = ?, ValidationKey = ?, ValidationSignature = ?`,
  binds context-first.
- `formation-approval.ts`: `FormationApprovalRequest.peerId`→`peerKey`; `vouchFields`
  destructure renamed; wire-contract doc + header docs updated (joiner mints nonce).
  `FormationVouchFields` is an `Omit` so it followed automatically.
- `control-formation-recorder.ts`: `obtainApproval` no longer mints (request type
  `Omit<FormationApprovalRequest, 'validationUrl'> & { validationUrl: string | null }`,
  returns `{ validationKey?; validationSignature? }`); `recordUsage` and
  `provisionAndRecord` take `peerKey`/`peerSignature`/`usageStampId` and thread them
  into `obtainApproval` + the database calls; stale writer-asserted doc rewritten.

**Phase 3 — partial.** `strand-solicitation.ts`: the `FormationUsageRecorder`
interface (`recordUsage` + `provisionAndRecord` params) is DONE — `peerKey`,
`peerSignature`, `usageStampId`, docs updated. Nothing else in Phase 3 is done.

**Current known compile errors (the work list, in order):**
- `strand-solicitation.ts:431` — `recordFormationComplete` still passes `peerId`.
- `strand-formation-manager.ts` — `recordUsage` call (~306–312) and
  `provisionAndRecord` call (~379–385) still pass `peerId: initiatorPartyId`.
- Every test file listed in `files:` (untouched; will break on the new required params).

## Remaining work

### strand-formation-protocol.ts
- `FormationContactMessage` (125–134) gains `peerKey: string` (initiator's base64url
  ed25519 public key — the key behind `partyId`, written to `FormationUsage.PeerKey`),
  `usageStampId: string` (joiner-minted single-use nonce), `peerSignature: string`
  (signature over the `'consent'` digest — see `formationConsentMessage`). `partyId`
  stays (human-facing identity in logs / `FormStrandResult.memberKey`).
- `FormationListenerOptions.provisionStrand` (296): widen to
  `(contact: FormationContactMessage, signal?: AbortSignal)`; `provision()` (395–398)
  passes `contact` through instead of unpacking token/partyId/disclosure.
- Consent pre-check in `runSession` between the contact log (474) and `validateToken`
  (476) — cheapest first; discloses nothing. Reject via
  `send({ approved: false, reason: 'Invalid joiner consent' })` when ANY of:
  - `requireEd25519PublicKeyB64(contact.peerKey, 'joiner peer key')` throws — CATCH
    and do NOT echo the value (its error message embeds the whole rejected string;
    its own NOTE says to cap the echo for remote input — this is that case; log at
    debug without the raw value).
  - `ed25519PublicKeyB64FromPeerId(contact.partyId)` returns null or ≠
    `contact.peerKey` — keeps `partyId` honest; lives only here (SQL can't unwrap a
    multihash; `partyId` isn't stored).
  - `verifyFormationConsent({ token: contact.token, usageStampId:
    contact.usageStampId, peerKey: contact.peerKey, disclosure:
    canonicalJson(contact.disclosure), peerSig: contact.peerSignature })` is false.
  DB constraint stays the authority — this is the legibility pre-check, exactly as
  `verifyFormationApproval` is for the approver.
- **Cycle check DONE this run**: `seed-bootstrap.ts` imports only control-stream,
  types, control-database, canonical-json, peer-authorization, seed-trust-policy,
  trusted-owner-store — never protocol/manager/solicitation, so importing
  `ed25519PublicKeyB64FromPeerId` (seed-bootstrap.ts:87, `(peerId: string) =>
  string | null`, never throws) into the protocol is cycle-free. Imports from
  `peer-authorization.js`, `ed25519-key.js`, `canonical-json.js` also clean.

### strand-formation-manager.ts
- `formStrand` (181–221) gains a
  `consent: { peerKey: string; usageStampId: string; peerSignature: string }` param
  (solicitation mints/signs; manager only places it on the contact object built at
  188–193). `memberKey: contact.partyId` (210) unchanged. Direct-manager tests break
  exactly here — they must supply real key material.
- Constructor listener adapter (136–137): `(contact, signal) =>
  this.provisionAsResponder(contact, signal)`.
- `provisionAsResponder` (282–347): signature becomes
  `(contact: FormationContactMessage, signal?)`; `JSON.stringify(disclosure)` (291) →
  `canonicalJson(contact.disclosure)` (import from `./canonical-json.js`), updating
  the "Serialized ONCE" comment to "CANONICALLY serialized — both sides derive the
  identical string"; the `recordUsage` call (306–312) and `provisionUnbound`'s
  `provisionAndRecord` call (379–385) pass `peerKey: contact.peerKey, peerSignature:
  contact.peerSignature, usageStampId: contact.usageStampId`. `provisionUnbound`'s
  provisioner/placeholder branches (393–407) still take `initiatorPartyId` — read it
  off `contact.partyId`. The catch mapping (326–346) UNCHANGED — confirm a joiner
  nonce-reuse `unique` failure lands in the generic 'Formation conflict, retry' arm
  with the invite unspent.

### strand-solicitation.ts
- `formStrand` (289–335): after the keypair mint (299–304: `generateKeyPair('Ed25519')`
  → `peerIdFromPrivateKey` → `memberKey = peerId.toString()`), add:
  `ed25519KeyPairFromLibp2p(privateKey)` (ed25519-key.ts:34, takes the libp2p
  `PrivateKey`, returns `{ privateKeyB64, publicKeyB64 }`) →
  `usageStampId = generateStampId(memberKey)` → build
  `fullDisclosure = { ...disclosure, partyId: memberKey }` ONCE (canonicalize AFTER
  the partyId override — this object is what the responder receives and writes) →
  `disclosureText = canonicalJson(fullDisclosure)` → sign
  `formationConsentMessage({ token, usageStampId, peerKey: publicKeyB64,
  disclosure: disclosureText })` with `sign(msg, privateKeyB64, 'ed25519', 'bytes',
  'base64url', 'base64url')` (import `sign` from `@optimystic/quereus-plugin-crypto`).
  Pass `fullDisclosure` plus `{ peerKey: publicKeyB64, usageStampId, peerSignature }`
  to `manager.formStrand` (which currently receives `{ ...disclosure, partyId:
  memberKey }` at 311–315 — replace with `fullDisclosure` + consent arg). New imports:
  `ed25519KeyPairFromLibp2p` (./ed25519-key.js), `generateStampId` +
  `formationConsentMessage` (./control-database.js — no cycle), `canonicalJson`
  (./canonical-json.js), `sign`. The no-node placeholder fallback (326–334) writes
  nothing — no signing.
- `recordFormationComplete` (424–434): integration-test mock helper only (per its own
  doc). Widen to satisfy the interface: take `peerKey` and
  `consent: { usageStampId: string; peerSignature: string }` (or fold flat) and pass
  through; update its callers in strand-solicitation.spec.ts (167–196:
  `service.recordFormationComplete('token-1', 'key-1', 'strand-1')`).

### Phase 4 — tests (untouched; caller sweep from run 3 still valid)
All in `packages/cadre-core/test/`. Recommend ONE shared helper (in the invite spec or
a small `test/formation-consent-helper.ts`): mint a throwaway seed
(`randomBytes`/known vector), derive pub via `ed25519PublicKeyFromPrivate`, mint
nonce, sign `formationConsentMessage` → `{ peerKey, usageStampId, peerSignature }`
spreadable into every call site. ~25 call sites want it.

- `control-formation-invite.spec.ts` (~1460 lines, heaviest): every
  `redeemInvitation`/`recordFormationUsage` call passes `peerId: 'peer-…'` (sites at
  170, 198, 218, 239, 257, 276, 291–292, 346, 376, 391, 406, 417, 431, 571, 600, 624,
  638–639, 662–663, 807, 822, 832, 868, 898, 918, 936, 958, 970, 1024, 1033, 1043,
  1054, 1079, 1085, 1098, 1117, 1126, 1146, 1155, 1169, 1179, 1194, 1202, 1221, 1243,
  1452); a raw `insert … with context PeerSignature = ?` block near ~501 (hand-rolled
  two-insert transaction) — rewrite to the new columns/context. The `Redemption`
  helper type ~724 spreads into `recordFormationUsage` — extend it once. The
  approver-digest cases (807+) sign `formationVouchMessage` — rename `peerId`→
  `peerKey` there and keep approval + consent over the SAME nonce and disclosure
  bytes. ADD the positive/negative consent pair here (valid `(PeerKey, PeerSig)`
  lands; sig by a different key rejected).
- `control-revocation-replay.spec.ts`: calls at 702, 732, 771, 780, 797, 809, 824,
  846, 862 — mechanical helper-spread.
- `control-formation-recorder.spec.ts`: mock ControlDatabase stubs
  `mintUsageStampId` (59) — delete that line (method no longer exists); `REDEMPTION`
  fixture + `recordUsage`/`provisionAndRecord` calls (67–154) gain the new required
  fields.
- `strand-formation-consent.spec.ts`: end-to-end scenarios (bound, unbound at
  289/441, ValidationUrl) — drives real managers, so the initiator path mints real
  consent via solicitation; expect mock-recorder param updates + any hand-built
  contacts to need real key material.
- `strand-solicitation.spec.ts`: mock recorders (85, 172, 227) get the widened param
  type (structural — `async () => {}` mocks may compile as-is);
  `recordFormationComplete` callers (183, 196) per its new signature.
- Integration scenarios (`packages/integration-tests`, e.g.
  `multi-party-workflows.integration.ts`, `strand-formation-e2e.integration.ts`):
  hand-written `partyId: 'party-b-excl-…'` inputs are SAFE — solicitation overrides
  `partyId` with the minted memberKey. `yarn build` surfaces any typed mock recorders
  there.

Gate: `yarn build`, `yarn lint`, cadre-core unit suite, formation integration
scenarios — all green, streamed through `tee`. Then hand off to review/ per the
implement-stage rules; the exhaustive negative matrix + docs (`docs/api.md` hook-body
rename) remain `debt-formation-consent-tests-docs`.

## Edge cases to hold (settled design — do not re-open)

- Nonce reuse by the joiner → second insert fails `unique` → surfaces as
  'Formation conflict, retry' with the invite unspent, not a dropped frame.
- ValidationUrl invite needs BOTH signatures, over the same nonce + same canonical
  disclosure bytes.
- `redeemInvitation` runs its two inserts with deferred CHECKs; `PeerConsented` has
  no subquery so it is NOT deferred — confirm it evaluates correctly inside that
  transaction.
- Concurrent redemptions of one multi-use invite: loser's consent signature must stay
  re-presentable under a new use number (free-nonce property) — do not bar it.
- `FormationAbortedError` / settle-grace paths unchanged; a consent-check rejection is
  an ordinary `approved: false`, never an abort.
- Listener pre-check runs for ALL responder configs (including provisioner-only /
  no-recorder fallbacks) — their tests must send well-formed contacts even though
  those branches write no `FormationUsage` row.
