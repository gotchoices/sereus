description: Write down which network shapes our tests actually cover and which they don't, so the next missing shape is visible instead of sitting unnoticed for months.
prereq: harness-topology-builder
files: docs/testing.md
difficulty: easy
----

# Topology coverage map in `docs/testing.md`

`docs/testing.md` documents gate policy but says nothing about which network *topologies* the
integration suite covers. That is why gaps sat unnoticed: no strand above three machines, no
multi-machine party on either side of a cross-party strand, no relayed strand at all (audited
2026-09-07 across all integration scenarios — the numbers live in the two topology tickets that
came out of that audit).

Add a short section — a **map, not a status board**: one line per topology class, each line
naming the class and pointing at the scenario file that covers it, or naming it uncovered and
pointing at its ticket slug. No pass/fail state — consistent with that document's own rule that
current state lives in the suites and in `tickets/`.

Classes to name (verify each pointer against the tree before writing it; adjust wording, drop
or add classes as the tree says — the list below is the audit's, not gospel):

- Single machine, control plane only — e.g. `control-write-while-alone-convergence.integration.ts`.
- Two-machine party, control plane (both write orderings) —
  `control-db-two-node-convergence.integration.ts`, `control-write-degraded-cohort-member.integration.ts`.
- Three-machine party, control plane — `control-cohort-three-node-isolation.integration.ts`,
  `harness-party-control-cohort.integration.ts` (the `TestParty` star world).
- One party, two machines, one strand — `websocket-chat.integration.ts`,
  `convergence-stress.integration.ts`, `strand-addr-seed-convergence.integration.ts`,
  `strand-late-cadre-join.integration.ts` (join-after-founding ordering).
- Cross-party strand, one machine per party (two and three parties) —
  `strand-formation-e2e.integration.ts`, `strand-membership-closed-strand-e2e.integration.ts`,
  `rbac-signed-write.integration.ts`, `multi-party-workflows.integration.ts`.
- Cross-process nodes (child processes, fixed port bands) —
  `deliver-seed-cross-network.integration.ts` (via `child-node-fixtures.ts`).
- Harness self-coverage of the topology builder — `harness-topology.integration.ts`
  (exists once the prereq lands; that is the only reason for the prereq).
- **Uncovered**: cross-party strand with multi-machine parties (four machines, the strand
  replication breadth) — ticket `scenario-two-multi-machine-cadres-share-one-strand`.
- **Uncovered**: medium private network — ticket `feat-scenario-medium-private-network`.
- **Uncovered**: public open strand network — ticket `feat-scenario-public-open-strand-network`.
- **Uncovered**: relayed strand / NAT reachability — ticket `strand-network-nat-relay-reachability`.

One more line in the same section: the time-budget rule of thumb for topology scenarios
(explicit per-hook timeouts, roughly linear in machine count, every strand member is a second
libp2p node) with a pointer at the `topology.ts` module header where the full statement lives —
a pointer, not a copy.

## Edge cases & interactions

- A ticket slug named as "uncovered → ticket X" may later be completed and swept from the
  board; that is fine — the map names intent, and the landing scenario replaces the slug when
  someone next touches the section. Don't design around it.
- Keep the section out of the gate-policy narrative — it is reference material; place it as its
  own `##` heading near the end.

## TODO

- Verify every scenario pointer and ticket slug above against the current tree/board.
- Write the section into `docs/testing.md`.
- `yarn lint` (markdown untouched by ESLint, but run the repo's normal check anyway to be sure
  nothing else drifted).
