description: When someone joins one of a party's networks, the joiner now signs its own join record and the database verifies that signature before accepting. Production code is finished; this ticket updates the database-level unit test suites to supply real signing keys.
files: packages/cadre-core/test/formation-consent-helper.ts (DONE), packages/cadre-core/test/control-formation-invite.spec.ts (DONE), packages/cadre-core/test/control-revocation-replay.spec.ts (DONE), packages/cadre-core/test/control-formation-recorder.spec.ts (DONE), packages/cadre-core/src/control-database.ts (read-only reference)
difficulty: medium
----

# Joiner-signed formation records — DB-level test sweep (part 1 of 2)

Ninth run. Run eight COMPLETED ALL EDITING — every file listed above is fully
converted; the `peerId`/`PeerId` grep is CLEAN across all three specs (comments
included). `yarn workspace @serfab/cadre-core build` was run and passed. The
ONLY remaining work is the test gate: run the three spec files, fix any
fallout, then hand off to review/.

Part 2 (`debt-formation-consent-tests-strand`) covers strand-layer specs,
integration call sites, repo-wide gate — do NOT touch those here.

## DONE (do not redo, do not revert)

- `test/formation-consent-helper.ts` — complete (prior run). Exports
  `TestJoiner`, `mintJoiner()`, `JoinerConsent`, `signJoinerConsent`,
  `mintConsent(token, disclosure = '', joiner = mintJoiner())`.
- `test/control-formation-invite.spec.ts` — complete. All mechanical
  `peerId:` → `...mintConsent(token)` conversions; `rawInsertFormationUsage`
  8-column rework; `Redemption` interface now carries
  `peerKey`/`peerSignature`/`peerPrivateKey` with `reconsent()` helper;
  `sibling`/`validatingInvite` mint + sign real joiners; fresh-nonce,
  peer-swap (retitled "PeerKey alone"), tampered-disclosure cases `reconsent`
  their varied field; cross-strand/cross-token/verbatim/race untouched per
  design; seating + novalidation cases rebuilt with explicit consent and
  stale writer-mints-nonce comments reworded; row selects/asserts now
  `PeerKey`; comment sweep PeerId→PeerKey done; NEW
  `FormationUsage.PeerConsented joiner-consent branch` describe added
  (positive land + wrong-key record refusal + wrong-key seating rollback,
  each asserting `'PeerConsented'` and unchanged counts); recorder DB-backed
  and `hasOutstandingFormationInvite` describes converted.
- `test/control-revocation-replay.spec.ts` — complete. Helper import added;
  all nine formation sites spread `...mintConsent(<site's token var>)`
  (`token`×4, `first`, `second`, `bound`, `token`, `bound`). CadrePeer
  `peerId` usages intentionally untouched.
- `test/control-formation-recorder.spec.ts` — complete. `mintUsageStampId`
  deleted from the booby-trap DB; `REDEMPTION` fixture and every inline
  `recordUsage`/`provisionAndRecord` call now carry
  `peerKey: 'joiner-pub-key'`, `peerSignature: 'joiner-consent-sig'`,
  `usageStampId: 'stamp-1'` (abort tests — plausible strings suffice, nothing
  verifies consent).
- `yarn workspace @serfab/cadre-core build` — run, green.

## REMAINING — the gate

```
yarn workspace @serfab/cadre-core test control-formation-invite control-revocation-replay control-formation-recorder 2>&1 | tee /tmp/consent-db-tests.log
```

Fix any failures (all edits are in tests, so failures mean a test edit is
wrong, not production code). Watch-list if something reds:

- The new PeerConsented describe makes assumptions confirmed on run seven but
  never executed: negative record case expects `'PeerConsented'` as single
  rejector with an owner-signed strand pre-inserted; the redeemInvitation
  negative expects the strand insert to roll back (check-on-insert fires at
  the usage insert inside the txn).
- `reconsent` cases: if a "PeerConsented fires first" failure appears on a
  case expecting `'Authorized'`, that case forgot to re-sign a consent-digest
  field (digest covers Token, UsageStampId, PeerKey, Disclosure).
- Do NOT run repo-wide `yarn build` / `yarn lint` — strand specs +
  integration files stay broken until part 2; expected and theirs.

When green: write the review/ handoff (distilled summary, honest about the
fact that the PeerConsented negatives were authored this sweep and deserve
reviewer scrutiny) and delete this ticket.

## Settled design — do not re-open

- Nonce reuse → `UsageStampId` `unique`; verbatim replay case pins it.
- ValidationUrl invite needs BOTH signatures over the same nonce + the same
  disclosure bytes.
- Race loser's consent stays re-presentable under a new use number (UseNumber
  outside the consent digest); race test pins the vouch side.
- Excess-property checks don't fire on spread properties — spreading a full
  `Redemption` (incl. `peerPrivateKey`) into calls is TS-clean.
