----
description: Approvals that admins sign to add or remove people in a group can currently be captured and used again later, so someone who is removed can be silently put back and someone who is re-added can be silently removed again. Make each approval usable exactly once, for exactly one action.
files: schemas/strand.qsql, packages/quereus-plugin-sereus/src/strand-schema.ts (byte-identical mirror), packages/cadre-core/src/strand-membership-writer.ts, packages/cadre-core/src/index.ts, packages/cadre-core/test/strand-member-revocation.spec.ts, packages/cadre-core/test/strand-membership-peer-rotation.spec.ts, packages/cadre-core/test/strand-membership-writer.spec.ts, packages/cadre-core/test/strand-membership-invite.spec.ts, packages/quereus-plugin-sereus/test/strand-schema-drift.spec.ts, packages/integration-tests/src/scenarios/strand-membership-closed-strand-e2e.integration.ts
difficulty: hard
----

# Strand membership approvals: make each one single-use and action-scoped

<!-- resume-note -->
EIGHTH run, budget-capped mid-Phase-4b. Runs 1–3 orientation; 4 schema; 5 writers; 6
transaction-nesting fix; 7 proof run + generation fix + peer-rotation helpers; THIS run
(8): **Phase 4a COMPLETE** (peer-rotation spec fully converted, zero references to the
deleted signers — verified by grep) and **Phase 4b MID-EDIT** (revocation spec: imports +
helpers landed, 7 call sites still red at typecheck).

## Run-7 PROVEN facts (do not re-derive; proven by a now-deleted throwaway spec)

- `unique` on `StampId` enforced (Member and MemberPeer): valid-signature insert reusing
  a live stamp rejected.
- `RevocationRecorded` rejects valid-signature delete with no tombstone; writer
  `revokeMember` round-trip (delete + tombstone, one txn) passes — same-txn sibling
  visibility works.
- `NotRevoked` rejects insert naming a retired stamp even with genuinely valid fresh
  signature; re-add under fresh stamp works.
- `addManager` round-trips: promotion at generation 1 seats (parity proven).
- Join-mode `inStrandTransaction`: resign+revoke composition (two writers inside one
  caller txn) ACCEPTS; addManager+revokeMember composition rejects `/Authorized/`.
- After `revokeMember`, re-presenting the ORIGINAL add signature WITH original stamp has
  Authorized PASS — `NotRevoked` is the ONLY rejector.

## Load-bearing learnings (runs 4–7)

1. **Crypto plugin digest is FRAMED and TYPE-TAGGED** (optimystic
   `quereus-plugin-crypto/src/crypto.ts` `encodeField`): INTEGER 1 and TEXT '1' never
   collide. `signStrandApproval` takes `ReadonlyArray<string | number>`; `addManager`
   signs the NUMBER `generation`. **Every test signing a promotion digest must pass the
   generation as a NUMBER, not a string.** No schema change was needed for this.
2. **Stale dist bites**: cadre-core tests resolve `@serfab/quereus-plugin-sereus` via its
   built `dist/`. If the schema mirror is EVER edited again, `yarn build` in
   `packages/quereus-plugin-sereus` (fast, esbuild) before running cadre-core tests. It
   was rebuilt run 7 and the mirror is final — should not recur.
3. **Pin-preserving tombstones**: `Revocation.Authorized` shares the constraint NAME
   `Authorized` with the Member/Manager/MemberPeer gates. A raw negative DELETE pinning
   `/Authorized/` needs a same-transaction tombstone (else `RevocationRecorded` may
   report first). Tombstone signer may even be the attacker: a non-committed-member
   signer fails `Revocation.Authorized` → message still `CHECK constraint failed:
   Authorized`. Writer-driven negative tests file tombstones internally — existing
   writer-driven pins hold unchanged.
4. `@quereus/quereus` "Cannot find module" IDE diagnostics on writer/spec files are
   language-server noise (linked-workspace resolution) — vitest resolves fine. Typecheck
   in Phase 5 is the arbiter.

## Tree state RIGHT NOW

- `schemas/strand.qsql` + `packages/quereus-plugin-sereus/src/strand-schema.ts` — final,
  byte-identical, dist rebuilt run 7. **Do not touch.**
- `packages/cadre-core/src/strand-membership-writer.ts` — final, proven end-to-end run 7.
  **Do not touch.** `packages/cadre-core/src/index.ts` — exports correct.
- `packages/cadre-core/test/strand-membership-peer-rotation.spec.ts` — **CONVERSION
  COMPLETE this run.** All 15 planned sites converted per the run-7 site plan: raw
  Manager/MemberPeer inserts mint + carry StampId; raw deletes pinning `/Authorized/`
  carry same-txn tombstones (spec-local `fileTombstone`); `insertManagerRow` mints stamps
  and signs 5-element NUMBER-generation digests; the generation-binding replay test
  reuses ONE stamp across the rejected gen-2 and accepted gen-1 inserts; stale
  domain-separation comments rewritten. `grep
  'signStrandPayload|signStrandMemberPeerRemoval'` → zero matches. **NOT yet executed —
  suite unverified.**
- `packages/cadre-core/test/strand-member-revocation.spec.ts` — **MID-EDIT, red at
  typecheck**:
  - Imports swapped: `signStrandApproval` + `generateStrandStampId` in,
    `signStrandMemberAction` out.
  - Helpers LANDED (between `insertHeader` and the describe blocks):
    `memberStampId(db, key)` — unfiltered Member scan → StampId;
    `fileTombstone(db, stampId, retiree)` — NOTE signature: TableName 'Member' is baked
    into the SQL, only (stampId, retiree) are params;
    `rawDeleteMember(db, key, context, retiree?)` — NEW 4th optional param: when given,
    fetches the target's live stamp, wraps delete + retiree-signed tombstone in
    `inTransaction`; when omitted, bare delete as before;
    `rawInsertMember` now inserts `(Key, StampId)`, minting a fresh stamp.
  - **7 unconverted call sites, all still calling `signStrandMemberAction`, at CURRENT
    line numbers: :269, :289, :314, :334, :455, :481, :498** (typecheck diagnostics
    confirm exactly these — the helper block shifted everything ~42 lines down).
- Every other file untouched. `tickets/backlog/bug-strand-approval-domain-separation.md`
  still exists — delete at handoff.

## Decisions locked into landed schema+writers (tests must match)

| Rule | Signed digest (variadic; each element one digest arg) |
|---|---|
| Member add-by-manager | `'Strand.Member','add',new.Key,new.StampId` |
| Member remove-by-manager | `'Strand.Member','remove',old.Key,old.StampId` |
| Member self-departure | `'Strand.Member','leave',old.Key,old.StampId` |
| MemberPeer self register | `'Strand.MemberPeer','add',new.MemberKey,new.PeerId,new.StampId` |
| MemberPeer self remove | `'Strand.MemberPeer','remove',old.MemberKey,old.PeerId,old.StampId` |
| MemberPeer manager remove | `'Strand.MemberPeer','manager-remove',old.MemberKey,old.PeerId,old.StampId` |
| Manager promotion | `'Strand.Manager','add',new.MemberKey,new.Generation,new.StampId` — Generation as NUMBER |
| Manager self-resignation | `'Strand.Manager','resign',old.MemberKey,old.StampId` |
| Manager removal-by-another | `'Strand.Manager','remove',old.MemberKey,old.StampId` |
| Revocation tombstone | `'Strand.Revocation','retire',new.TableName,new.StampId` |

Delete writers quiet-no-op (log + return) when target row absent. `Revocation` context
`(MemberKey, Signature)` non-null; `Authorized` reads `committed.Member`.

## Phase 4b — finish revocation spec (START HERE; 7 sites, walked this run)

All helpers exist — each site is a mechanical swap. "retiree founder" below means pass
`founder` as `rawDeleteMember`'s 4th arg (tombstone keeps the `/Authorized/` pin
truthful, learning 3).

- :269 stranger-signs-with-real-ManagerKey: `managerSignature = signStrandApproval(
  ['Strand.Member','remove', member.publicKeyB64, await memberStampId(db,
  member.publicKeyB64)], stranger.privateKeyB64)`; retiree founder.
- :289 approval-minted-for-DIFFERENT-member: sign `['Strand.Member','remove',
  approved.publicKeyB64, await memberStampId(db, approved.publicKeyB64)]` with founder
  key (a GENUINE approval for `approved`), present on `victim`'s delete; retiree founder
  (tombstone goes over VICTIM's stamp — rawDeleteMember handles that itself).
- :314 multi-row delete: fetch BOTH targets' live stamps first; signature over
  `approved`'s (key, stamp); wrap the raw `db.exec` delete in `inTransaction` and file
  founder-signed tombstones for BOTH stamps inside it → `/Authorized/` pin stays clean
  (per-row evaluation: `collateral`'s row finds no branch).
- :334 stranger-binds-itself: sign `['Strand.Member','remove', member.publicKeyB64,
  await memberStampId(db, member.publicKeyB64)]` with stranger key; retiree founder.
- :455 C-signs-B-departure: `signStrandApproval(['Strand.Member','leave',
  memberB.publicKeyB64, await memberStampId(db, memberB.publicKeyB64)],
  memberC.privateKeyB64)` — perfect leave-tagged digest, wrong signer; retiree founder.
  Update the comment ("remove-tagged payload" → leave-tagged digest over the live stamp).
- :481 add-replayed-as-remove: re-mint the admission signature —
  `signStrandApproval(['Strand.Member','add', member.publicKeyB64, await
  memberStampId(db, member.publicKeyB64)], founder.privateKeyB64)` (equals what
  addMemberByManager signed: same fields, row still carries that stamp); retiree founder.
- :498 remove-replayed-as-add: `const freshStamp = generateStrandStampId();` sign
  `['Strand.Member','remove', target.publicKeyB64, freshStamp]` with founder key; the
  raw insert becomes `insert into Strand.Member (Key, StampId) ... values (?, ?)`
  carrying `[target.publicKeyB64, freshStamp]`; keep the `/Authorized/` pin.

Untouched-by-design in this spec: mass-delete and wipe-then-seat tests keep loose
`/CHECK constraint failed/` pins, no tombstones needed; `rawInsertMember` sites (founding
seats, revoked-joiner re-insert) work as-is now that it mints stamps — revoked-joiner
still pins `/Authorized/` because the FRESH stamp passes NotRevoked and only the gate
rejects.

## Phase 4c — remaining files (plan from runs 4–7, still accurate)

- `strand-membership-writer.spec.ts`: signer renames only (`signStrandMemberAction` /
  removed helpers → `signStrandApproval`); hydration tests keep passing. Grep for the
  old names is the authoritative list.
- `strand-membership-invite.spec.ts`: likely zero edits (invite handshake unchanged —
  still `signStrandPayload`-based, which remains exported for invites).
- Integration `strand-membership-closed-strand-e2e.integration.ts`: writer calls
  (:247–320) absorb free; raw MemberPeer impostor insert (:311–317) needs minted StampId
  + 'add'-tagged digest so rejection stays `Authorized` not NOT-NULL; check whether the
  `signStrandPayload` import is still used (invite path) — else drop it.
- `strand-schema-drift.spec.ts`: should pass as-is (mirror byte-identical); no edit
  expected.

## Phase 5 — validation + handoff

- `yarn vitest run` in `packages/cadre-core` and `packages/quereus-plugin-sereus`, root
  `yarn typecheck` + `yarn lint` — ALL streamed `2>&1 | tee` (runner kills on 10-min
  idle). Peer-rotation suite has NEVER been run post-conversion — expect possible
  first-run fixups there, then revocation.
- Delete `tickets/backlog/bug-strand-approval-domain-separation.md`; say so in handoff.
- Review handoff must note: digest table above; TYPE-TAGGED digest framing + NUMBER-typed
  Generation (learning 1) and where pinned; non-null Revocation context divergence;
  scan-not-seek stamp reads; quiet-no-op deletes; inStrandTransaction join-mode decision
  + rationale (writer docstring has it); pin-preserving tombstone idiom (learning 3);
  dist-rebuild footgun (learning 2).
<!-- /resume-note -->

**SCOPE SPLIT (unchanged):** new R1–R6 replay-pin spec, five-element parity case, and
`Revocation`-growth tripwire NOTE live in
`tickets/implement/3.55-strand-approval-replay-pins.md`
(`prereq: strand-approval-stamps-and-tags` — keep this ticket's slug). THIS ticket =
schema + writers + keeping every EXISTING suite green.

## Background (original analysis, condensed)

Every signed write in the `Strand` schema is authorized by an ed25519 signature over a
digest of the affected key(s). Before the schema change, nothing in the signed bytes said
which incarnation of the row the approval was minted for, and nothing marked an approval
as spent; context values travel with the write to the strand's peers, so any party that
saw a membership write held a permanently valid approval. Six replays (R1–R6: manager
re-seat/re-demote/forced-resign, member re-admit/re-evict, peer unbind/rebind) were
reproduced on a real closed strand. The landed schema closes them: insert replays die on
`NotRevoked` (stamp retired permanently at delete), delete replays die because the delete
digest binds `old.StampId` and a re-created row carries a fresh stamp.

Already fine, do not "fix": `Invite`/`ConsumedInvite` (insert-only, fresh PKs — no
stamps); `Member.Authorized`'s invite-branch freshness clause;
`StrandMemberVerifier`/`strand-member-registry.ts` (off-engine self-proof only — leave the
whole file alone).

## Related, deliberately NOT in scope

- `strand-approval-replay-pins` (implement, prereq this) — R1–R6 explicit attack pins,
  five-element parity case, Revocation-growth tripwire NOTE
- `4-bug-devicetoken-authority-antireplay` (in `fix/`) — same treatment for `DeviceToken`
- `3.6-bug-strand-invite-no-revocation` (in `fix/`) — may reuse this `Revocation` table;
  keep the table general enough that it can

## End
Work ticket as described above.
Do NOT commit — runner handles commits after you complete.
