services:
  bootstrap:
    build: .
    container_name: sereus_libp2p_bootstrap
    env_file:
      - ./env.example
    ports:
      - "4001:4001/tcp"
    volumes:
      - bootstrap_data:/data
    restart: unless-stopped

volumes:
  bootstrap_data:


