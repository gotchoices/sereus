services:
  bootstrap_relay:
    build: .
    container_name: sereus_libp2p_bootstrap_relay
    env_file:
      - ./env.example
    ports:
      - "4001:4001/tcp"
    volumes:
      - ./data:/data
    restart: unless-stopped


