# NOTE: This is a template. It will not run until you provide an actual image/command.

# Container image that runs a headless Optimystic node.
SEREUS_NODE_IMAGE=

# Where to persist node state/keys
DATA_DIR=/data

# Optional: how the node joins/enrolls into a cadre (TBD)
CADRE_ENROLLMENT_TOKEN=
CADRE_ID=


