# Where to persist the libp2p private key (stable PeerID across restarts)
DATA_DIR=/data
KEY_FILE=/data/libp2p-private.key.pb

# Listen address inside container
LISTEN_TCP=0.0.0.0:4001

# Optional: force advertised public addresses (comma-separated multiaddrs).
# Useful when running behind NAT or when container IP differs from public IP.
# Example:
# ANNOUNCE_ADDRS=/ip4/203.0.113.10/tcp/4001
ANNOUNCE_ADDRS=


