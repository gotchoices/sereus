# Release notes — pending

- **The `@optimystic/*` dependencies now require `^1.2.0`.** 1.2.0 already said it needed 1.2.0 but still accepted `^1.1.0`, so an app whose lockfile pinned optimystic 1.1.0 kept the slower commit path. No code changes.
